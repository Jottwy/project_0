# STATE.md — Estado vivo del proyecto
> Actualizado por /checkpoint al cierre de cada sesión. Leído al inicio de cada sesión.

## Última sesión
- Fecha: 2026-07-29 (cierre)
- Hecho: **Ronda de variedad de pasillo NORMAL (Piezas A-F) COMPLETA, LAS 6 CONFIRMADAS EN PLAYTEST** — techo (A, `225172f`), pipes (B, `cb9b04b`), paleta de tints por tile (C, `3732da9`), iluminación con cadencia (D, `59f6696`), props pegados a pared (E, `9656733`), humedad en suelo/pared (F, `bb40717`). Detalle completo en ▸ Variedad de pasillo NORMAL, Piezas A-F más abajo. Pieza geométrica "G" (jitter de densidad en NORMAL) queda APARCADA a propósito — ver ▸ Deuda `connect_zone_to_maze`. Medias paredes planificadas pero NO iniciadas (paso previo de viabilidad sin responder) — ver ▸ Deuda. De paso, diagnosticado (sin fix) un bug de respawn tras autosave en momento de muerte — ver ▸ Deuda.
- Fecha: 2026-07-07
- Hecho: **TODO CONFIRMADO EN PLAYTEST REAL (jugando, no solo tests): ADR-030 (consume_item) — el bug de muerte inevitable por inanición silenciosa queda RESUELTO (comer/beber restaura hunger/thirst del servidor de verdad); loot scatter "Opción A" (caches de items + zonas de carryables, retry 3 min) confirmado aterrizando en el suelo real tras el fix de RaycastUpOffset (causa raíz: el rayo nacía por encima del techo de 4 m e impactaba la capa equivocada — Y logueadas 4.0/8.0 en vez de -1.0); cofres de mundo "Opción B" (enmienda ADR-028, `is_chest` sobre `world.corpses`) — 4 cofres sembrados y LOOTEADOS correctamente, misma infraestructura que corpse sin tocar protocolo.** Backend 355/355 tests, clippy limpio en código nuevo, release desplegado (`Builds/Backend/`, 2026-07-07, verificado `grep -aoc consume_item/is_chest/spawn_world_chest`=2). Detalle en ▸ ADR-030 + loot y en DECISIONS.md (ADR-030 nuevo; enmienda world-chests de ADR-028).
- Además, re-confirmado en el mismo playtest lo que la sesión anterior dejó verificado: ADR-029 (PvP V0) loop completo hit→validación→daño→muerte→corpse→loot E2E con melee y firearm; fix de grounding de proxies (visual); ADR-026 parcial (Y real del cliente) + Opción C (compensación en emisor) sin doble descuento.
- Sesión previa (2026-07-04/06): ADR-029 Fases 1–3 + allowlist real; grounding de proxies; ADR-026 parte 3 + Opción C — ver ▸ secciones de estado y DECISIONS.md. Anterior (2026-07-03/04): ADR-028 corpse spawn COMPLETO (Fases A–E3 + pulido; wire v7→v10) — commits `a001c89`..`db5a335`; incidente del truncado de DECISIONS.md documentado ahí (regla: appends solo con Edit anclado directo).
- (2026-07-28) **ADR-031 (cama de respawn) y ADR-032 (persistencia de mundo V0 + sus 2 enmiendas) VERIFICADOS EN CÓDIGO, ya no son "diseño validado sin confirmar"**: backend en `backend/src/game_loop.rs` (`BED_DEF_ID`/`resolve_respawn`/`try_bed_spawn` para ADR-031; `AUTOSAVE_EVERY`/`resolve_save_path`/`load_or_fresh`/handler `save_and_shutdown` para ADR-032) y `backend/src/persistence/save.rs` (`save_world`); cliente en `Assets/Scripts/Network/InventoryReporter.cs` e `InventoryRestorer.cs` (enmienda de inventario STP real de ADR-032) + `NetworkInitializer.cs` (`TryRequestBackendSave`/`SaveOnQuitTimeoutMs` para save-on-quit) + `ProtocolActionTypes.cs` (`SaveAndShutdown`/`ReportInventory`). Confirmación por lectura de código, no por playtest nuevo.
- (2026-07-28) **Todo el código backend de ADR-029→032 estaba VERIFICADO en código pero SIN COMMITEAR** (huérfano en el working tree desde su sesión original) — cerrado hoy en 3 commits separados: `6ac7b66` (feature completo, 10 archivos, sin tocar formato), `ab6681a` (cargo fmt puro sobre game_loop.rs/world/mod.rs, cero cambios de comportamiento, separado para no mezclar reformateo con feature work), `8fd0fd2`+`90a7487` (instrumentación TP_WATCH nueva, ver abajo). Ver también el commit de docs `3cd1293` (ADR-025→032 en DECISIONS.md/STATE.md) que ya había cerrado la brecha de documentación por separado.
- (2026-07-28) **Instrumentación TP_WATCH ampliada (sin fix todavía)**: (a) `MPTRACE step=CHUNK_DISPLACEMENT tick=<n> chunk_id=(x,z) old_seed=<y> new_seed=<z>` en `World::tick_teleportation` (world/mod.rs) — permite cruzar timestamps de chunk displacement LEGÍTIMO (mecánica núcleo, ver CLAUDE.md) contra TPs reportados por el jugador, para descartar falsos positivos. (b) `TP_WATCH` ahora loguea `in_window=<bool>` explícito — antes había que inferirlo indirectamente del comentario del código; ahora se aproxima comparando el tick actual contra `Player.last_reposition_tick` (nuevo campo diagnóstico, `#[serde(skip)]`, sellado en los 3 eventos que arman la ventana del cliente: `session_restored`/`player_died`/`player_respawned`) con una ventana de 21 ticks (0.35s·TICK_HZ, espejo de `AuthoritativePoseApplier.SnapWindow`). Aproximación por construcción — el backend no conoce el timer real del cliente. **Hallazgo de código durante esta instrumentación (sin confirmar aún con logs reales)**: el propio comentario de `TP_WATCH` ya razona que su fuente rastreada (`resolve_move_clamp`) NO puede explicar un TP visible del cliente FUERA de la ventana armada, porque `AuthoritativePoseApplier.OnMovementDelta` ignora el stream de delta fuera de esa ventana (`AuthoritativePoseApplier.cs:250-251`) — si los logs reales confirman que los TPs reportados caen fuera de ventana, la causa NO está en este camino y el diagnóstico debe ampliarse a otra superficie.
- (2026-07-29) **Pieza 3 (zone_kind → loot) CABLEADA Y CONFIRMADA EN PLAYTEST** (auditoría de timing previa confirmó que la carrera de la Pieza 2 NO aplica igual aquí — ver detalle abajo en ▸ zone_kind → estética + loot): `ChunkLootRoll.RollItems`/`RollCarryables` ganan un parámetro `ZoneLootProfile` (struct plano, sigue sin `using UnityEngine`, headless-testable) que varía chance de cache/zona + pesos de pool + rareza de arma + mix de material — **nunca el número de slots** (restricción dura: `ItemsPerCache`/`CarryablesPerZone` siguen const fijas, para no descoindexar `_collectedItems`/`_collectedCarry` de `ChunkLootManager`, keyed por `(cx,cz,slot)`). `ZoneLootTable` (nuevo SO, `Assets/Scripts/Gameplay/GridWorld/ZoneLootTable.cs`, patrón GripPoseSet vía `Assets/Editor/ZoneLootTableCreator.cs` — crear-si-falta, nunca re-siembra) mapea 1:1 los 12 `zone_kind`; entrada 0 (NORMAL, ~83% del mundo explorado) es idéntica byte-a-byte a las constantes planas pre-Pieza-3. Gate de zona en `ChunkLootManager.CollectLoads`: `ZoneRegistry.TryGetZone` SIN timeout (a diferencia de `ZoneReadyOrExpired` del streamer, que expira a 0.75s para no dejar agujero de suelo) — el loot no tiene esa urgencia, así que una columna con zona desconocida simplemente queda sin sellar y se reintenta cada `scanInterval` (0.25s) hasta que `ZoneRegistry` responda; decisión explícita, `ZoneReadyOrExpired` NO se extrajo/reutilizó. **Fix separado y commiteado aparte** (revertible sin tocar el loot): `ZoneRegistry.Refresh` ahora ignora `cv.layer != 0` — antes, en columnas multi-capa (el cluster de spawn V30A entre ellas), el último write ganaba la capa MÁS ALTA en vez de la 0 donde pisa el jugador, así que tanto el tint (Pieza 2) como el loot (Pieza 3) habrían leído la zona equivocada justo en el área más visible del primer playtest. Tests: 7 tests existentes de `ChunkLootRollTests.cs` migrados a la nueva firma (13 call sites) + 4 tests nuevos (invariante de conteo de slots por perfil para items y carryables, forma de la tabla de 12 entradas, bounds-safety de `ZoneLootTable.Profile`). **CONFIRMADO EN PLAYTEST:** variación visual de zona (verde/marrón/gris/rojo) y variación de loot (0–16 items por columna según zona) funcionando correctamente. Log temporal `[ZoneLootDiag]` YA RETIRADO tras la confirmación (commit `e8b72d4`), mismo criterio que `[ZoneTintDiag]`. **Setup de repo:** el asset generado (`Assets/Resources/Loot/ZoneLootTable.asset`) quedó COMMITEADO (`b3793e7`) — un clon fresco lo trae listo, sin pasos manuales; si llegara a faltar (borrado, mal trackeado), `ChunkLootManager` cae silenciosamente en `ZoneLootProfile.Default` para toda zona, sin error visible — no confiar solo en el fallback, verificar que el archivo sigue en git. **Deuda declarada, fuera de alcance de esta sesión:** `StpChestSpawner` (los 4 cofres de mundo, ADR-028 Opción B) NO está gateado por `ChunkLootManager.Enabled` y tiene sus propios pools duplicados — seguirá soltando loot uniforme, ajeno a la zona, junto a caches que sí la respetan; el balance fino de los 12 perfiles (`ChunkLootRoll.DefaultZoneLootProfiles`, todavía los defaults sin retocar) es TODO(balance) explícito, primer pase deliberadamente no final; suite EditMode compile-verificada pero corrida por el humano junto al playtest, no headlessly por el agente. Commits de la Pieza 3: `cf1ab94` (fix capa-0 ZoneRegistry) → `391471d` (ZoneLootProfile/ZoneLootTable/gate) → `a07bfbd` (docs) → `e8b72d4` (drop diag log) → `b3793e7` (asset commiteado).
- (2026-07-29) **ADR-033 + enmienda Pillar (zone_kind → densidad geométrica en grid_gen, opción c nibble alto) COMPLETO, CONFIRMADO EN PLAYTEST VISUAL** — columnas PILLAR_HALL visibles y correctamente posicionadas (chunk (3,2), zone_kind 5, capturado en HUD). Diagnóstico temporal `[ZoneDensityDiag]` (commit `d481c01`) YA RETIRADO tras la confirmación, mismo criterio que `[ZoneTintDiag]`/`[ZoneLootDiag]` de la Pieza 2/3. Detalle completo en ▸ ADR-033 + enmienda Pillar más abajo.
- (2026-07-28) **RESUELTO el mismo día — Gate `cargo clippy -D warnings` del hook `postedit-check-rust.py` estaba ROTO a nivel de repo** (deuda preexistente, no introducida ese día): ~54 warnings únicos repartidos en 13 archivos (`world/architecture/chunk_generator.rs`, `world/architecture/layout_grammars.rs`, `world/graph/verticality.rs`, `world/grid_gen/tile_walls.rs`, `world/grid_gen/generator.rs`, `world/grid_gen/tests.rs`, `world/levels/level_0/builder.rs`, `world/levels/level_0/v30a_showcase.rs`, `world/levels/level_0/level0_golden_slice.rs`, `world/volumetric_grid.rs`, `world/collision.rs`, `network/{sync,mod,protocol}.rs`, `persistence/save.rs`, `game_loop.rs`) — mayormente `manual_is_multiple_of`/`unnecessary_map_or`/`len_zero`/`needless_range_loop` de una versión de Rust/clippy más reciente que cuando se escribió ese código. Corregido en commits separados por archivo/módulo; los 3 `too_many_arguments` genuinos (`game_loop.rs::process_authoritative_interaction`, `volumetric_grid.rs::push_macro_access`, `v30a_showcase.rs::inter_layer_volume`) se dejaron con `#[allow(clippy::too_many_arguments)]` + `TODO(refactor)` en vez de refactor a struct de parámetros (decisión: cero riesgo, el refactor de firma queda para sesión propia). `cargo clippy --all-targets -- -D warnings` limpio, `cargo build` y `cargo test` (372/372) en verde tras el fix — confirmado también que la suite de `world::collision::tests` (resolve_move/resolve_safe_spawn, 39 tests) pasa intacta.

## Estado actual — Variedad de pasillo NORMAL, Piezas A-F — COMPLETO, LAS 6 PIEZAS CONFIRMADAS EN PLAYTEST (2026-07-29)
- **Qué hace:** auditoría de viabilidad previa (misma sesión) identificó que el ~83% del mundo explorable (zona NORMAL) se sentía repetitivo porque toda la estética es POR CAPA (`LayerVisualConfig` compartido por los 4 layers), no por chunk/tile — y que varios sistemas ya construidos y probados estaban simplemente APAGADOS en layer 0 (donde vive el jugador). Las 6 piezas encienden/extienden esos sistemas sin tocar `grid_gen` ni el wire — mismo patrón client-only que las Piezas 1-3 de zone_kind.
- **Pieza A — variedad de techo en layer 0 (`225172f`):** `ceilingPanelVariety` 0 → 0.25 en `Layer0_Vestibulo.asset` (sistema Fase 5B ya existente, activo en layers 1-3 desde 0.40 pero apagado en layer 0). Solo dato. **CONFIRMADO EN PLAYTEST:** panel ausente y panel colgando con tilt observados, sin lectura como agujero/error.
- **Pieza B — pipes en layer 0 (`cb9b04b`):** `ceilingPipes`/`ceilingPipeChance` 0 → 1/0.35 en el mismo asset. Solo dato.
- **Pieza C — paleta de tints por tile (`3732da9`):** `LayerVisualConfig` += `floorTintVariants`/`wallTintVariants`/`ceilingTintVariants` (array vacío = tinte plano de siempre, layers 1-3 quedan byte-idénticos), elegidos por `Hash01` en coordenadas GLOBALES de tile con salt propio por rol. Paleta autorada solo en layer 0 (3 tonos piso, 4 pared, 3 techo — desviaciones discretas sobre el tinte base, el jitter de ±8% sigue aplicándose encima).
- **Pieza D — iluminación con ritmo (`59f6696`):** tres fixes en `PlaceFluorescentLights` — (1) recibe `walls` y ya no siembra luminarias dentro de tiles sólidos (mismo test `0x0F` de `PlaceProps`); (2) `lightDensity` gana jitter determinista por chunk (banda 0.30-1.20, asimétrica hacia abajo a propósito para que existan tramos oscuros, `TODO(balance)`); (3) `light.range` honra `cfg.lampRange` en vez de estar clavado a `5f`. Resuelto de paso: divergencia asset/creador en `lampRange` (asset vivo 5, creador 13/11/9/7 nunca aplicado) — las 4 capas quedan en 5/5.5/6/6 (alcance horizontal < 5 m de ancho de pasillo, sin sangrado lateral entre paredes).
- **Pieza E — props pegados a pared (`9656733`):** `PropEntry.canBeRotated == false` ya SIGNIFICABA "wall-aligned" por su propio tooltip, pero nada alineaba nada (muebles flotando en el centro del tile). Ahora un prop de suelo wall-aligned se apoya contra un lado con pared del propio tile (retranqueo 1.9 m), elegido por hash determinista. `propDensity` 0.12 → 0.18 en layer 0; pool ampliado con `cabinet`/`cable` (tipos YA existentes en `PlaceholderFactory`, cero assets nuevos).
- **Pieza F — humedad en suelo/pared (`bb40717`):** `MoistureAt` (ya usado solo en techo) gana salts por parámetro, así que suelo/pared/techo tienen campos de humedad INDEPENDIENTES (salt compartido haría que cada mancha de techo llevara una de suelo justo debajo — leería como bug de iluminación, no daño por agua). Salts del techo conservan sus literales originales — el techo queda byte-idéntico a lo confirmado en Pieza A. Multiplicador de color, NO decals (decals reales quedan fuera de esta fase, según la auditoría).
- **CONFIRMADO EN PLAYTEST (2026-07-29):** las 6 piezas verificadas juntas — variedad de techo, pipes, paleta de tints, iluminación con cadencia, props pegados a pared y humedad extendida, todas visibles y funcionando sin regresión.
- **Verificación:** compile-check Roslyn headless de `BackroomsSurvival`/`EditModeTests`/`Assembly-CSharp`/`Assembly-CSharp-Editor` en verde en cada pieza. Sin playtest por parte del agente en ninguna — las 6 fueron jugadas por el humano.
- **Deuda abierta de este bloque:** (a) banda de jitter de iluminación (Pieza D) calibrada a ojo, `TODO(balance)`; (b) paleta de tints (Pieza C) autorada solo para layer 0, layers 1-3 sin paleta propia; (c) `zoneTints` de `LayerVisualConfig` sigue deliberadamente SIN serializar en los 4 `.asset` (vive del inicializador de campo, paleta placeholder "no final") — re-ejecutar el menú "Backrooms ▸ Create Layer Visuals" hornearía eso; no ejecutar salvo que se quiera ese efecto.
- **Commits (orden cronológico):** `225172f` (Pieza A) → `cb9b04b` (Pieza B) → `3732da9` (Pieza C) → `59f6696` (Pieza D) → `9656733` (Pieza E) → `bb40717` (Pieza F).

## Próximo paso (UNO solo)
- **Cerrar la atribución de TP (teleports inexplicados)** — diagnóstico en curso desde la auditoría 2026-07-01; `TP_WATCH`/`RESOLVE_DIAG` siguen ACTIVOS en game_loop.rs (marcados "REMOVE after diagnosis"), ahora con `in_window` explícito y `CHUNK_DISPLACEMENT` cruzable (ver arriba, 2026-07-28) — el paso que falta es correr un playtest real con esta instrumentación y LEER los logs (nadie lo ha hecho todavía; todo el análisis previo es estático). Es el **gate real** para desbloquear las partes 1–2 de ADR-026 (migración de colisión XZ del jugador a `grid_gen` + re-snap de spawn), que siguen BLOQUEADAS — no tocar sin nueva validación humana. Ver ▸ Deuda conocida (2026-07-28, hallazgo ADR-018) para el contrato del bitmask de 5m y el estado exacto de esta migración cuando se retome.
- **Deuda: entidades PvE (Lurker/Crawler/Shadow) con daño DESACTIVADO temporalmente (2026-07-07)** — diagnóstico del día: eran la causa de las muertes silenciosas "caminando sin razón" (3 muertes con stats sanas; 10 HP/golpe cada 1 s a ≤2 m, `ATTACK_DAMAGE`/`ATTACK_COOLDOWN` en entity.rs; corroborado por la tasa de drain de sanity +0.3/entity en el chunk). Están implementadas en el backend (aggro, movimiento, ataque — todo eso SIGUE corriendo) pero el `EntityRenderer` nunca se cableó en la escena STP (invisibles) y el daño no tenía feedback (StatInterpolator aplica con `SetHealthSilent`; el evento `damage_taken` no tiene handler en Unity). Gate: `ENTITY_DAMAGE_ENABLED=false` en game_loop.rs (solo gatea la línea `take_damage`; los tests de mundo/entidad quedan intactos como cobertura para una reactivación). Pendiente decisión: integrar completamente (renderer + feedback + rebalance) o retirar del roadmap. **No tocar sin decisión explícita.**
- **Deuda (planificada en la ronda de variedad de pasillo NORMAL, 2026-07-29, NO INICIADA):** medias paredes — dos variantes de panel con altura parcial, (1) "knee wall" desde el suelo (se puede ver por encima) y (2) colgando desde el techo (tipo dintel/viga). El PASO PREVIO obligatorio (confirmar si es implementable como variante puramente visual del panel existente, elegida por hash determinista de tile — mismo patrón que los paneles de techo de Fase 5B — SIN que el backend necesite distinguir "pared completa" de "media pared" como conceptos distintos, lo cual sí requeriría espacio en el wire) **SIGUE SIN RESPUESTA**. No hay bit libre conocido en el bitmask de tile (los 8 bits de `walls[x][z]` están en uso: bajo = aristas, alto = nibble de pilares de ADR-033/Pillar). Si la respuesta al paso previo resulta ser NO, es un problema de la misma clase que el de `Pillar` y necesita su propia enmienda/ADR — no implementar apurado. **No tocar sin resolver primero ese paso previo.**
- Residuales visuales menores, verificables de forma oportunista en cualquier playtest (no bloquean nada): (a) ocultación del proxy al morir (`dead:bool`) — implementado, sin confirmación visual explícita; (b) grace-period del despawn del cadáver (`MissingReferenceException` del prompt vendor) — fix compilado, solo verificado en teoría. El relay P2P de cadáveres quedó CUBIERTO por el playtest PvP del 2026-07-06 (corpse visible y looteado en pantalla ajena).
- **Deuda (hallazgo 2026-07-29, sesión ADR-033/Pillar, FUERA de alcance de esa sesión): `connect_zone_to_maze` (`backend/src/world/grid_gen/generator.rs:579`) arrasa celdas `Pillar`/`Void` al carvar el túnel de reconexión hacia una zona sin vecino transitable** — viola el invariante "estampar gana" que el propio `generator.rs:345` documenta (el estampado de Fase 4 nunca debería perderse frente a pasadas posteriores). Ruta RARA (solo dispara cuando una zona abierta queda sin ningún vecino caminable tras el estampado), detectada durante la auditoría del nibble alto de pilares (ADR-033/Pillar, enmienda "Opción c") al verificar qué fases posteriores a la Fase 4 podían pisar celdas `Pillar`. No arreglada de paso (scope cerrado de esa sesión — era solo backend read-only para el resolver de zona_density y luego el nibble de render). Pendiente: sesión propia para decidir si el carvado de reconexión debe rodear pilares/voids en vez de atravesarlos, o si el invariante "estampar gana" se enmienda explícitamente para esta ruta. **SIGUE SIN TOCAR** (2026-07-29, cierre de la ronda de variedad de pasillo NORMAL) — es además el motivo por el que la Pieza geométrica "G" (jitter de densidad dentro de NORMAL, ver auditoría de esta misma sesión) quedó APARCADA: subir `num_open_zones`/`open_zone_size` en NORMAL aumentaría la frecuencia de esta ruta y extendería el bug del ~5% de chunks PILLAR_HALL al ~83% del mundo. No se retoma la Pieza G sin resolver esto primero.
- **Deuda (bug de respawn tras autosave en el momento exacto de la muerte, hallazgo 2026-07-29, diagnóstico sin confirmar):** un save de ADR-032 escrito mientras `health: 0.0` (autosave cayó justo tras una muerte, antes de pulsar respawn) deja al jugador en LIMBO al recargar la sesión — no muestra `DeathUI` ni permite un respawn limpio, en vez del comportamiento correcto esperado por diseño ("recargar muerto con botón de respawn disponible", que ES lo que ADR-032 promete). Dos hipótesis SIN CONFIRMAR por falta de logs de la sesión real donde ocurrió: **(a)** `resolve_respawn` (`game_loop.rs:1519`) + `player.respawn_point` persistido (saco de dormir, ADR-031) resolviendo contra el `World` recién generado de la seed (`World::new`, `game_loop.rs:219`/`239`) en vez del mundo persistido con los edificios STP restaurados (`net.stp_buildings` sí se hidrata, pero el grid de colisión de `world` no los conoce) — el punto de respawn podría no resolver a una celda transitable coherente con lo que el jugador ve. **(b)** desincronía de flags LOCALES del cliente (`RespawnRequester._nativeRespawnFired`/`_serverSawDead` en `Assets/Scripts/Gameplay/RespawnRequester.cs`) — no persistidos, arrancan en estado "vivo" en cada sesión nueva, mientras el backend ya reporta `is_dead()` verdadero desde el tick 0 tras hidratar el save; el puente hacia `DeathUI`/`AuthoritativePoseApplier` podría no dispararse correctamente en el primer frame. **Próximo paso cuando se retome: reproducir y capturar `MPTRACE step=RESPAWN` (`game_loop.rs:1756`/`1769`) del log del backend en el momento exacto del bug, ANTES de diagnosticar más** — sin esos logs no se puede descartar (a) ni (b). No tocar sin esa evidencia.

## Estado actual — zone_kind → estética + loot (Piezas 1+2+3) — COMPLETO, LAS 3 PIEZAS CONFIRMADAS EN PLAYTEST (2026-07-28/29)
- **Qué hace:** la clasificación de zona que el backend ya calculaba (`zone_kind: u8`, 12 valores `ZONE_*` en `backend/src/world/chunk/surface_profiles.rs`) ahora AFECTA la apariencia del mundo. Antes el dato existía y viajaba por IPC pero **nadie lo consumía** salvo un HUD de debug (`PoiDebugHud`). Cliente-only salvo el fix de generación; sin cambio de protocolo, sin ADR.
- **Fix de backend previo, `8addd3b`** (prerrequisito descubierto en auditoría): `generate_chunk_layer` (`world/generator.rs`, el camino de expansión infinita) elegía `template_id` temático pero NUNCA derivaba `zone_kind` de él → todo chunk fuera del set finito de `generate_initial_structures` reportaba `ZONE_NORMAL`. Ahora aplica `template_zone_kind(template_id)`, espejo del fallback que ya usaba el camino de estructuras. Test de regresión `expansion_chunk_zone_kind_matches_template` (barrido 40×40, exige ≥1 zona no-NORMAL). `cargo clippy --all-targets -D warnings` limpio, 373/373 tests.
- **Pieza 1 — `ZoneRegistry` (`Assets/Scripts/Gameplay/ZoneRegistry.cs`, commit `f88496d`):** registro estático `Dictionary<(cx,cz), byte>` alimentado desde `IPCClient.LatestState.visibleChunks` (`ChunkView`) por un pump auto-arrancado con `RuntimeInitializeOnLoadMethod` — sin cableado de escena ni prefab. Clave por XZ solamente (`zone_kind` ignora la layer vertical). **Deliberadamente NO colgado de `ChunkVisualLifecycle`/`ChunkRenderer`**: ese renderer está desactivado en `GameBootstrap.cs:15` y no corre en el boot real; el vivo es `ChunkStreamer`. Verificado en runtime con variedad real (zonas 0/4/5/6/8 observadas).
- **Pieza 2 — tint por zona (`36e97b5`):** `LayerVisualConfig` += `Color[] zoneTints` (12 entradas, tonos placeholder deliberadamente distinguibles — **no es el arte final**) + `ZoneTint(int)` bounds-safe con fallback a blanco. `GridChunkBuilder.BuildFromWalls` multiplica ese tint en la ruta de pintado por-tile YA existente (`MaterialPropertyBlock`, la misma del jitter tile-a-tile) para floor/wall/ceiling. **No toca geometría, props, pipes, lámparas ni colisión.**
- **Carrera de mensajes resuelta (`61cad5c`) — el bug real de la Pieza 2:** `ChunkData` (geometría grid_gen) y `WorldState` (`ChunkView`, único portador de `zone_kind`) son mensajes IPC INDEPENDIENTES sin orden garantizado, y los chunks se construyen UNA vez y no se re-tintan → casi todos quedaban blancos para siempre. Diagnosticado con log temporal (`found=False` en casi todos; el único `found=True` tintaba bien). Solución: `ChunkStreamer` retiene el chunk si no conoce su zona y reintenta desde `_wallsCache` el frame siguiente, reutilizando el contrato "sigue encolado" de un envío caído (sin re-request, sin rehacer geometría); `ZoneWaitTimeout = 0.75 s` lo construye en blanco si la zona nunca llega, así que **nunca puede quedar un agujero**. El **anti-fall guard (chunk bajo el jugador) está EXENTO del gate** y construye de inmediato como siempre. Se arregla en el momento del build (no re-tintando después) para evitar el parpadeo blanco→color, y basta porque `zone_kind` NO cambia en runtime (ver ▸ Deuda conocida, hallazgo `tick_teleportation`).
- **Verificado en playtest (2026-07-28):** tint visible y diferenciado por zona, **sin parpadeo** blanco→color, **sin retraso perceptible** en el chunk de spawn, `found=True` en la mayoría de chunks. Logs temporales de diagnóstico (`[ZoneTintDiag]`, `[ZoneRegistry]`) YA RETIRADOS tras la confirmación.
- **Fix de capa 0 en `ZoneRegistry` (2026-07-29, commit separado):** `Refresh` indexaba por `(cx,cz)` sin filtrar `layer` — el backend ordena `visibleChunks` por `(x, layer, z)`, así que en columnas multi-capa (el cluster de spawn V30A entre ellas) el último write ganaba la capa MÁS ALTA, no la 0 donde pisa el jugador. `TryGetZone` nunca devolvía `false` en ese caso — el valor no faltaba, era el equivocado, así que ningún gate existente (`ZoneWaitTimeout`, el nuevo gate de Pieza 3) podía detectarlo. Ahora `Refresh` ignora `cv.layer != 0`. Afecta tanto al tint (Pieza 2, cambia el resultado visual ya verificado en el cluster de spawn — por eso va en commit propio, revertible sin tocar loot) como al loot (Pieza 3).
- **Pieza 3 — loot por zona (`391471d`, gate) + `cf1ab94` (fix capa-0) + `b3793e7` (asset commiteado):** `ChunkLootRoll.RollItems`/`RollCarryables` ganan un parámetro `ZoneLootProfile` (struct plano, sigue sin `using UnityEngine`) que varía chance de cache/zona + pesos de pool + rareza de arma + mix de material — **nunca el número de slots** (`ItemsPerCache`/`CarryablesPerZone` siguen const fijas, restricción dura para no descoindexar `_collectedItems`/`_collectedCarry` de `ChunkLootManager`, keyed por `(cx,cz,slot)`). `ZoneLootTable` (nuevo SO en `Assets/Scripts/Gameplay/GridWorld/ZoneLootTable.cs`) mapea 1:1 los 12 `zone_kind`, creado vía `Assets/Editor/ZoneLootTableCreator.cs` (patrón GripPoseSet: crear-si-falta, nunca re-siembra — a diferencia de `BackroomsLayerVisualsCreator`). Entrada 0 (NORMAL, ~83% del mundo por la vía de expansión) es idéntica a las constantes planas pre-Pieza-3. Auditoría de timing previa (sesión 2026-07-28) confirmó que la carrera de mensajes de la Pieza 2 NO se traslada igual aquí: el loot exige `ipc.LatestState != null` (la misma precondición que puebla `ZoneRegistry`) antes de rodar nada, así que en arranque y caminando la zona siempre está disponible antes del roll; solo teleport >2 chunks y la restauración de sesión ADR-032 son ventanas reales. Por eso el gate de `CollectLoads` (`ZoneRegistry.TryGetZone`) es SIN timeout — a diferencia de `ZoneReadyOrExpired` del streamer (expira a 0.75s para no dejar agujero de suelo), una columna de loot sin zona conocida simplemente no se sella y se reintenta cada `scanInterval` (0.25s); `ZoneReadyOrExpired` deliberadamente NO se extrajo/reutilizó. **Setup de repo:** `Assets/Resources/Loot/ZoneLootTable.asset` (los 12 perfiles default, sin retocar todavía) quedó COMMITEADO — un clon fresco no necesita correr el menú manualmente; si el archivo llegara a faltar, `ChunkLootManager` cae silenciosamente en `ZoneLootProfile.Default` para toda zona, sin error visible, así que conviene verificar por git que sigue trackeado en vez de confiar solo en el fallback. Spawns de entidades (Lurker/Crawler/Shadow) quedan FUERA, como estaba decidido.
- **Verificado en playtest (2026-07-29):** variación visual de zona (verde/marrón/gris/rojo) y variación de loot (0–16 items por columna según zona) funcionando correctamente. Log temporal `[ZoneLootDiag]` YA RETIRADO (`e8b72d4`) tras la confirmación, mismo criterio que `[ZoneTintDiag]`.
- **Commits, las 3 piezas, 3 sesiones:** Pieza 1 — `f88496d`. Pieza 2 — `36e97b5` (tint), `61cad5c` (fix carrera), `2c1ee58` (drop diag log). Pieza 3 — `cf1ab94` (fix capa-0), `391471d` (ZoneLootProfile/ZoneLootTable/gate), `a07bfbd` (docs), `e8b72d4` (drop diag log), `b3793e7` (asset commiteado).
- **Deuda abierta de este bloque:** (a) los 12 tonos y los 12 perfiles de loot son placeholder, falta pasada de arte/balance real por zona (props específicos por zona ni empezados; `ChunkLootRoll.DefaultZoneLootProfiles` es TODO(balance) explícito, el `.asset` commiteado todavía son esos defaults sin rebalancear); (b) el tint/loot son capas de "mood" sobre geometría generada por grid_gen — un chunk `ZONE_STORAGE` se tinta y lootea como almacén pero no tiene FORMA de almacén (son dos subsistemas de mundo distintos, ver ▸ Deuda conocida hallazgo ADR-018); (c) Opción C (llevar `zone_kind` en `GridChunkData`) eliminaría la carrera de raíz — requiere ADR, ver ▸ Deuda conocida; (d) `StpChestSpawner` (4 cofres de mundo, ADR-028 Opción B) NO está gateado por `ChunkLootManager.Enabled` y duplica sus propios pools — sigue soltando loot uniforme ajeno a la zona, deliberadamente fuera de alcance de la Pieza 3; (e) suite EditMode compile-verificada (Roslyn headless, 3 assemblies en verde) pero corrida por el humano en el Test Runner del editor, no headlessly por el agente.

## Estado actual — ADR-033 + enmienda Pillar (zone_kind → densidad geométrica en grid_gen, opción c: nibble alto) — COMPLETO, CONFIRMADO EN PLAYTEST VISUAL (2026-07-29)
- **Qué hace:** extiende el bloque anterior (zone_kind → estética/loot, capas CLIENTE) a la GEOMETRÍA real que genera `grid_gen`. Antes, `zone_kind` no influía en nada de lo que produce el generador — `ZONE_PILLAR_HALL` era una etiqueta sin efecto geométrico pese a que la Fase 4 "stamp wins" de `generator.rs` ya sabe estampar salones con pilares. Acotado, como fija ADR-033, a las rutas de RENDER y del ROBAPIELES; el jugador real sigue sin colisión con `grid_gen` (deuda aceptada, ver ADR-026 partes 1–2 bloqueadas) — sin cambio de protocolo/wire hasta la enmienda Pillar, que sí lo amplía (ver abajo) de forma retrocompatible.
- **Resolver puro (`world/zone_density.rs`, commit `e904f7c`):** `rules_for(world_seed, cx, cz, layer)` re-deriva `zone_kind` de `(world_seed, cx, cz, layer)` — NUNCA lee la `World` viva (opción R3 híbrida descartada: rompería el determinismo entre peers). Consulta primero las estructuras iniciales (cubre el cluster de spawn V30A) y cae al sorteo de plantilla espejado de `world::generator` si no hay estructura. Solo `ZONE_PILLAR_HALL` cambia el perfil (`num_open_zones`/`open_zone_size`/`pillar_chance` al nivel de "El Caos", el más abierto ya calibrado); los otros 11 `zone_kind` quedan inertes, `TODO(balance)` explícito — primer pase incremental, no big-bang.
- **Cableado render + robapieles (commit `58c0408`):** `chunk_tile_walls`/`GridGenChunkCache` reciben el resolutor inyectado (`RulesFn`, firma pública en `grid_gen`, implementación en `world/`) — `grid_gen` sigue sin importar `world/`. Render y colisión del fantasma usan EL MISMO resolutor: la garantía de no-divergencia de ADR-018 se conserva.
- **Test de conectividad entre densidades vecinas (commit `8f47f0b`, Paso 1 bloqueante):** ningún test previo ejercitaba la costura con perfiles DISTINTOS entre chunks vecinos de la misma capa — bloque 3×3 en tablero de ajedrez (perfil más cerrado vs más abierto) confirma conectividad global + travesía en cada borde compartido. Asimetría de paneles de borde medida en **0 de 840 aristas** (el borde reservado sólido + la apertura canónica compartida hacen la costura simétrica con independencia del perfil).
- **HALLAZGO EN PLAYTEST (2026-07-29): `CellType::Pillar` NO era representable en el wire.** `GridChunkData.walls` es un bitmask de ARISTAS de 5 m (`[[u8;10];10]`, solo `0x0F`); una celda-pilar de 2.5 m bloquea una de las dos sub-travesías de cada arista, pero la otra suele quedar transitable → la arista sale abierta igual y el pilar se evapora en la cuantización. `BuildFromWalls` nunca instanciaba `prefabs.pillar` (código muerto desde Fase 4.2); `ClassifyTile`/`HasPillar` pertenecía a la ruta local ya eliminada. Confirmado en playtest: salones PILLAR_HALL sin columnas visibles, con huecos donde deberían estar (documentado en DECISIONS.md, enmienda ADR-033/Pillar, commit `b321bc1`).
- **Opción (b) descartada — props client-side deterministas — por su propia cláusula de parada, ANTES de escribir código:** no hay una "función de siembra" de pilares aislable (un solo `StdRng` consumido secuencialmente por las 4 fases de `generate_layer`, offset data-dependent); `StdRng` no es reproducible ni entre versiones de `rand`; no existe infraestructura de test cruzado Rust↔C#. Replicarlo en C# habría exigido portar el generador completo, con riesgo real de "casi coincide" (columnas dibujadas al lado de la celda sólida real contra la que el robapieles sí colisiona) — peor que no implementarlo.
- **Opción (c) — nibble alto de `walls[x][z]` (DECIDIDA, commit `533b224`; implementada commits `1bc2088` backend + `ac8bdf0` cliente):** los 4 bits `0xF0`, antes sin uso, marcan qué sub-celda de 2.5 m del tile es `CellType::Pillar` (`0x10` NW, `0x20` NE, `0x40` SW, `0x80` SE). Calculado en `tile_walls_from_grid` (no en el wrapper — crítico para que `render_and_phantom_agree_on_pillar_hall` siga probando la no-divergencia real), criterio ESTRICTO `kind()==Pillar` (nunca `is_solid()`, que dibujaría columnas en agujeros/paredes). Mismo tipo, mismo tamaño de wire, sin bump de `WIRE_SCHEMA_VERSION`; el código existente que ya enmascaraba con `0x0F` (`PlaceProps`) sigue funcionando sin tocarlo. `BuildFromWalls` instancia `prefabs.pillar` (prefab ya existente, sin collider — colisión sigue exclusivamente en Rust) en el centro de cada sub-celda marcada; guarda de null explícita (loggea una vez, nunca `NullReferenceException`); `PlaceProps` excluye tiles con cualquier bit de pilar del spawn de props (evita clipping). Tintado con `mats.wall` en este primer pase — sin material propio, decisión de arte separada.
- **Tests:** coincidencia bit-a-bit del nibble contra celdas `Pillar` reales en chunks PILLAR_HALL genuinos; los 4 `assert_eq!` de bitmask preexistentes de `tile_walls.rs` intactos y verificados en verde (el criterio estricto los deja byte-idénticos); invariante de borde (tiles 0/9 nunca llevan nibble alto); conectividad propia del perfil PILLAR_HALL (`pillar_hall_chunks_stay_connected_and_non_degenerate`, hueco de cobertura que `all_walkable_cells_are_connected` no cubría). Lado C#: round-trip msgpack REAL con los 4 bits incluido `0x80` (primer valor de este campo que cruza a la rama `case 0xcc` del decoder); `BuildFromWalls` con instanciación real de GameObjects (mismo patrón que `RemotePlayerManagerTests`), con y sin bits, más el caso de prefab ausente. 385/385 tests Rust verdes, `cargo clippy --all-targets -D warnings` limpio; compile-check Roslyn headless de `BackroomsSurvival`+`EditModeTests` en verde.
- **CONFIRMADO EN PLAYTEST (2026-07-29):** columnas visibles y correctamente posicionadas en chunk (3,2), zone_kind 5, capturado en HUD. Diagnóstico temporal `[ZoneDensityDiag]` (introducido en `d481c01` para diferenciar en runtime si la geometría venía de `grid_gen`/ADR-033 o del legacy volumétrico) YA RETIRADO tras la confirmación.
- **Deuda abierta de este bloque:** (a) los otros 11 `zone_kind` siguen sin efecto geométrico, `TODO(balance)` explícito — PILLAR_HALL es la única prueba de concepto; (b) el jugador real sigue sin colisión contra `grid_gen` (ADR-026 partes 1–2 bloqueadas por la atribución de TP) — cuando se desbloqueen, heredará el cambio de densidad automáticamente, sin rehacer nada; (c) `connect_zone_to_maze` (`generator.rs:530-586`) arrasa celdas `Pillar`/`Void` al reconectar zonas aisladas, violando el invariante "estampar gana" — hallazgo de esta sesión, fuera de alcance, ver ▸ Próximo paso arriba; (d) tintado de pilar con `mats.wall` compartido, sin material propio — pendiente pasada de arte.
- **Commits (orden cronológico):** `46edc2c` (ADR-033 doc) → `8f47f0b` (Paso 1, test densidades vecinas) → `e904f7c` (resolver puro) → `58c0408` (cableado render+robapieles) → `05a7ba8` (test conectividad PILLAR_HALL) → `b321bc1` (enmienda: hallazgo Pillar no representable + descarte de opción b) → `533b224` (enmienda: decisión final opción c) → `1bc2088` (nibble alto backend) → `ac8bdf0` (render cliente) → `d481c01` (diag temporal, ya retirado en el commit de cierre de esta entrada).

## Estado actual — ADR-030 consumo + loot de mundo (Opción A scatter + Opción B cofres) — COMPLETO, VERIFICADO EN PLAYTEST REAL (2026-07-07)
- **ADR-030 `consume_item` (VALIDADA e IMPLEMENTADA, ver DECISIONS.md):** nueva acción IPC cliente→su-propio-backend. Diagnóstico previo: hunger/thirst del servidor solo subían vía `on_respawn()` (los managers STP locales están desactivados por `StatInterpolator` y cualquier cambio local se revertía en el siguiente snapshot) → TODA sesión moría por inanición/deshidratación silenciosa; la teoría "muerte por alejarse del host" quedó DESCARTADA (no existe ningún chequeo de distancia que dañe). Solución: `NetworkedConsumeAction` (clase propia, cero ediciones a STP — reemplaza la REFERENCIA `_actions[]` en los 7 assets de item consumible reales: Apple/Cooked Meat/Energy Bar/Large+Small Food Can/Water Bottle/Antibiotics) reporta `consume_item {item_id}`; el backend aplica restauración FIJA por item (`consumable_spec`, punto medio de los rangos `ConsumeData` ya autorados; `restore_hunger/thirst/health` con clamp [0,100]). Trust-the-client para posesión (no hay inventario autoritativo — decidido con el humano); sin dedupe (canal TCP ordenado, como report_damage); desperdicio en 100/100 permitido (clamp silencioso). **CONFIRMADO EN PLAYTEST: comer/beber restaura los stats del servidor.**
- **Fix de altura del loot scatter (causa raíz):** `TryFindWalkablePoint` usaba `RaycastUpOffset=5` — el origen del rayo quedaba POR ENCIMA del techo de 4 m (LAYER_HEIGHT) e impactaba su cara superior o el suelo de la capa siguiente (Y logueadas exactas: 4.0 y 8.0; el suelo real es -1.0; con cascada: zona centrada en Y=4 raycasteaba items desde 9 → aterrizaban en 8). Fix: `RaycastUpOffset` 5→1 y `RaycastDownRange` 10→3, espejo del `ProxyGroundingHook` validado (`_rayUp=1/_rayDown=3`, origen siempre bajo el techo). Aplicado a `StpItemSpawner`, `StpCarryableSpawner` y el nuevo `StpChestSpawner`. **CONFIRMADO EN PLAYTEST: caches y zonas aterrizan en el suelo real.**
- **Opción A (scatter, ya existente) confirmada en playtest** tras el fix: 6 caches de items + 4 zonas de carryables (Log/Stone/Metal), retry cada 10 s durante 3 min mientras el host explora (ventana render ~150 m).
- **Opción B (cofres de mundo, enmienda ADR-028):** `world.corpses` admite entries con `CorpseData.is_chest: bool` (`serde(default)`, additive — SIN bump de wire; un peer viejo renderiza el cofre como cadáver, degradación cosmética). TODA la maquinaria corpse reutilizada sin tocar: relay `CorpseList` a joiners, `take_corpse_item` + hop P2P + dedupe, despawn-por-vacío (cofre se vacía y desaparece, sin respawn en V0), persistencia fuera del ciclo de chunks. Siembra: `StpChestSpawner` (host-only, espejo de StpItemSpawner) envía `spawn_world_chest {request_id, position, items}` — 4 cofres, loot más rico que un cache (arma garantizada + munición + médicos + consumibles + materiales); dedupe de siembra por `(player, request_id)` REUTILIZANDO `processed_interactions` (request_ids en namespace alto fijo, estables entre re-envíos); regla post-E3 de loot vacío aplica. Visual: `CorpseSpawner` bifurca por `isChest` → crate STP real (`BuildingPieceDefinition` "Storage Crate" id 5498433 vía Resources; fallback cubo) neutralizado conservando colliders, con el mismo stack StorageStation/CorpseLootSync en un hijo dedicado — sin ragdoll/clothing/held/flujo de muerte. **CONFIRMADO EN PLAYTEST: 4 cofres sembrados y looteados correctamente.**
- **Verificación:** backend `cargo test` 355/355 (+3 tests: chest spawn/loot/despawn, gate+dedupe+empty de siembra, `is_chest` en round-trip P2P; +5 de ADR-030: allowlist ×2, clamps ×3); compile-check Roslyn headless de `BackroomsSurvival` y `Assembly-CSharp` en verde; release desplegado y verificado por grep en el binario.

## Estado actual — ADR-029 PvP V0 — COMPLETO Fases 1–3 + allowlist real, VERIFICADO E2E EN PLAYTEST (2026-07-06)
- **Loop completo confirmado JUGANDO (no solo tests): hit → validación host → daño aplicado por el backend de la víctima → muerte → corpse → LOOT.** Golpe/disparo con hacha y arma de fuego resta vida correctamente; la muerte por PvP genera el cadáver y ES LOOTEABLE en pantalla ajena. La Fase 4 del plan de ADR-029 (muerte/corpse/loot E2E) queda VERIFICADA — ADR-025/027/028 cubrieron la muerte PvP sin pegamento adicional, como el ADR predijo.
- **Arquitectura (ADR-029 + enmienda invuln en DECISIONS.md):** Unity NUNCA aplica daño PvP — `RemotePvpHitbox` (shim `IDamageHandler` instalado en runtime sobre los colliders del proxy; DESTRUYE los `CharacterHitbox` vendor pre-autorados que habrían aplicado daño real local) reporta `pvp_hit_candidate` por IPC con el `weapon_id` REAL del arma equipada. El host valida (orden fijo de 11 pasos: authority/self_hit/missing/dedupe/dead/invuln/arma/daño con clamp/dirección/distancia 3D/LoS-stub) → `PvpDamageGrant` fiable al backend de la víctima, que RE-VALIDA (dedupe defensivo + `invuln_until_tick`) y es el ÚNICO que llama `PlayerStats::take_damage`. Rechazos → `PvpHitRejected` + evento IPC al shooter (`PvpFeedbackController`, log-only). Dedupe con poda (`BoundedDedupeSet`, cap 512) en ambos lados. Wire: 3 `PacketPayload` nuevos (0x4A–0x4C).
- **Invulnerabilidad post-respawn (enmienda):** `PlayerStats.invuln_until_tick` = respawn + `RESPAWN_INVULN_TICKS` (180 ticks = 3 s a 60 Hz, derivado de TICK_HZ). Solo temporal por tick, sin zona espacial. Sin friendly fire/equipos en V0. Sin animaciones nuevas (flinch ADR-024 basta).
- **Allowlist real (8 armas STP, ids `DataIdReference` confirmados por lectura de assets):** Marlin 336 `9692212`, Wooden Bow `-7892144`, Bone Club `-1198406010`, Hunting Axe `2211292`, Hunting Knife `-9575342`, Steel Pickaxe `-1159981804`, Stone Spear `5085425`, Wooden Spear `-52379`. `weapon_id=0` siempre rechazado; fuera de lista → `invalid_weapon`.
- **DEUDA (no bloqueante, anotada):** (1) `victim_invulnerable` NO verificable por el host para víctimas remotas (el campo no viaja por el wire) — la víctima re-valida en su backend, NO es agujero de seguridad, pero el shooter no recibe rechazo explícito en ese caso (mismo síntoma que la ausencia de confirm para joiner-shooter, límite del contrato V0); exponerlo en la pose relay solo si Fase 5 lo necesita. (2) `max_damage`/`max_range` de la allowlist son placeholder `TODO(balance)`. (3) **Fase 5 (hitmarker/feedback visual real) NO implementada** — solo logging; explícitamente no se toca por ahora.

## Estado actual — Pose Y real + grounding de proxies (ADR-026 parcial + Opción C) — COMPLETO, verificado visualmente (2026-07-06)
- **Fix de grounding de proxies remotos:** el proxy flotaba ~1.8 m (bug doble: `ProxyGroundingHook` MUERTO por buscar hueso "Hips" — el rig solo tiene "Pelvis" — y offset de convención pivote). Resuelto: `FindBone("Pelvis")` + `RemotePlayerManager` resta `GridConstants.PlayerBaseY` (1.8, espejo documentado de `collision.rs::PLAYER_BASE_Y`, comentario cruzado en ambos lados; NO viaja por wire — exponerla sería ADR aparte) al aplicar la pose al root pivote-pies. Confirmado visualmente.
- **ADR-026 DESBLOQUEO PARCIAL — SOLO parte 3 (enmienda 2026-07-06 en DECISIONS.md):** el backend ya NO aplana la Y del jugador a `floor_player_y` — acepta la `claimed.y` del cliente si pasa el clamp de plausibilidad (`MAX_CLAIMED_Y_STEP = 3.0 m`/tick, precedente `ProxyJumpFeeder._verticalTeleportDistance`; < LAYER_HEIGHT 4 m; `TODO(balance)`); delta absurdo → fallback a floor SOLO ese tick, sin rechazar el movimiento. Gating XZ BIT-IDÉNTICO (sin tocar). Path de entidades (`resolve_move_simulated`, robapieles) conserva el floor-pin. PvP `too_far` se mantiene 3D a propósito (con Y real, 2D aceptaría golpes a través del techo). **Partes 1–2 SIGUEN BLOQUEADAS por la atribución de TP — no tocar sin nueva validación humana.**
- **Convención del canal de Y (Opción C, nota en la enmienda):** el primer playtest post-desbloqueo hundió los proxies ~1.8 m ("solo se veía la calva") por DOBLE DESCUENTO — el motor STP reporta pivote-pies (~0) y el receptor restaba `PlayerBaseY` a una Y que ya no llevaba la convención +1.8 del backend. Resuelto compensando EN EL EMISOR: `PlayerPoseTransmitter` suma `PlayerBaseY` a la Y enviada → el wire vuelve a significar "pies + PLAYER_BASE_Y" uniformemente para TODOS los emisores (jugadores compensados, robapieles/spawn/fallback nativos en esa convención). Confirmado en playtest. Único emisor que necesitó compensación (auditados todos los `Send*` con posición; `WorldInteractor` usa cámara — convención propia preexistente, sin cambio).
- **Consecuencia validada:** los 3 síntomas del playtest del 2026-07-06 (modelos no distinguibles por clustering — falso positivo de cámara; altura no dinámica en desniveles; salto remoto sin animación) quedaron diagnosticados y RESUELTOS por este bloque — la Y real llega al proxy y `ProxyJumpFeeder` detecta la velocidad vertical real sin cambios en el feeder.

## Estado actual — [G] Player Damage Sync (ADR-024) — slice cosmético IMPLEMENTADO (compile-green + commiteado), pendiente bake + play-test
- **Qué hace:** cuando un peer recibe daño LOCAL, su proxy 3P ejecuta un flinch cosmético. Siguiente eslabón del patrón de campo-de-pose de ADR-011/015/020/021/022/023; cosmético/host-relay, NO autoritativo. (Nota histórica SUPERADA 2026-07-06: decía "daño autoritativo real bloqueado por ADR-010" — el daño autoritativo PvP existe ya vía ADR-029, verificado E2E; `hit_seq` sigue siendo cosmético y ortogonal, como se diseñó.)
- **Decisión clave:** a diferencia de crouch/pitch/equipment/held_item (estados SOSTENIDOS leídos como nivel), un hit-reaction es un EVENTO transitorio re-disparable → campo TIPADO `hit_seq:u8` = CONTADOR MONOTÓNICO. El local lo incrementa por cada `DamageReceived`; viaja como nivel en cada pose; el proxy detecta el delta (`!=`) y dispara un flinch por incremento. Self-healing sobre UDP, late-joiner gratis (sentinela). NO se reusó el canal escalar `animation` (ADR-011 lo acota a una acción; colisionaría con pickup).
- **Origen de la señal (corrección de diseño):** evento `IHealthManager.DamageReceived` del character local, NO sondeo de `Health` — el `StatInterpolator` (ADR-009) escribe la salud vía `SetHealthSilent` (sin evento) → sondear el delta daría falsos flinches en reconciliación. `DamageReceived` solo dispara en daño real (`ReceiveDamage`).
- **Backend (v6→v7):** espejo exacto de `held_item` con `hit_seq:u8` en `Player`, `PlayerInput`, `RemotePlayerState`, `PacketPayload::PlayerUpdate` (+round-trip test `hit_seq=7`), `PeerConnection` (`update_player_state` SIN tocar → robapieles nunca flincha), `NetworkEvent::RemotePlayerUpdate`, `sync` (broadcast_player_update/peer_poses), `build_world_state`, `game_loop`. `WIRE_SCHEMA_VERSION` 6→7. `cargo test` 312/312; release GNU recompilado + copiado a `Builds/Backend/` (`grep -aoc hit_seq exe`=2).
- **Cliente TX:** `PlayerPoseTransmitter` se SUSCRIBE a `DamageReceived` del character local (incrementa `_hitSeq` byte wrapping; desuscribe en rig rebuild/OnDestroy — único campo EMPUJADO por evento, el resto se lee por-pose) → `IPCClient.SendPlayerInput(..., hitSeq)`, header MsgPack 15→16.
- **Cliente RX + proxy:** `RemotePlayerMsg`/`RemotePlayerView` += `hitSeq` (reset 0 en Acquire/Release). NUEVO `ProxyHitReactionHook` (view-lookup espejo de `ProxyCrouchHook` + rotación de bones por nombre espejo de `ProxyPitchHook`): cachea `UpperSpine`/`MiddleSpine`, change-detect con sentinela `int.MinValue` (primer sample no flincha → sin flinch al join/pool), en cambio arranca impulso decayente (~0.3s) que en `LateUpdate` aplica recoil ADITIVO al spine alrededor de `transform.right`. Removible. Cableado en `RemoteAvatarPrefabBuilder.WireHitReactionHook` (defaults `_magnitude=18`, `_recoverTime=0.3`).
- **PENDIENTE:** (1) bake — menú *Backrooms ▸ Build Remote Avatar Prefab* (añade el hook al prefab); no hecho (editor abierto). (2) play-test + calibración del recoil (`_magnitude`/`_recoverTime`/`_invert` en vivo). Slice 2 diferido (sin ADR nuevo): direccionalidad vía `DamageArgs`, clip real de flinch, escala por severidad.

## Deuda de arquitectura — ADR-009 L2 quedó a medias (CAUSA RAÍZ COMPARTIDA de P1 y P2)
> **RESUELTO (2026-07-03, ADR-025 Slice B + ADR-027 con adenda).** Se conserva como registro histórico del diagnóstico. Lo construido: consumidor de `delta_update` (AuthoritativePoseApplier, snap por ventana armada), report_damage (daño local → server), respawn-on-demand (botón → respawn_request), puente de flanco de muerte (muerte server-side silenciosa → Death nativo → DeathUI), puente UI de salud, auto-respawn de 15 s neutralizado. Todo validado E2E en runtime.
> Texto original (2026-06-30): descubierto al play-test de [G]. NO son bugs de [G] (que solo LEE `DamageReceived`). Preceden a hoy.
- **El hueco:** ADR-009 definía una capa L2 cliente con dos mitades. La de STATS (`StatInterpolator`) SÍ se construyó y está activa. La de MOVIMIENTO/POSICIÓN (`MovementReconciler` + reportar el daño local al servidor) se BORRÓ y NUNCA se reemplazó (lo documenta `PlayerPoseTransmitter.cs`: "replaces the deleted ... MovementReconciler stack"). El `delta_update` autoritativo del servidor se parsea, se encola y se drena (`IPCClient`), pero **NO existe ningún `AddMovementDeltaListener` en el proyecto** → la posición/velocidad autoritativa del servidor SE DESCARTA. El jugador local es de facto autoritativo de su posición; nada del backend se le aplica de vuelta.
- **P1 — salud salta/se cura cerca de la muerte (bug):** doble autoridad NO coordinada sobre `HealthManager._health`. (a) Servidor 5 Hz: `StatInterpolator.HealthBinder.SetHealthSilent(serverHealth)` (silencioso, sin `Death`/`Respawn`). (b) Local: `ReceiveDamage` (caída/peligro) baja vida y dispara `Death`, **pero NO se reporta al backend**. La salud server-side solo cambia por inanición/sed/fantasma → ignora el daño local → el siguiente tick `SetHealthSilent` lo restaura ("se cura al máximo"); la interpolación 2-muestras/50ms produce el flicker ("salta raro"); muerte local a 0 dispara `Death` pero `SetHealthSilent` puede restaurar vida>0 sin `Respawn` → estado inconsistente; muerte server-side llega silenciosa → `Death` nativo nunca se dispara.
- **P2 — respawn no aparece en sitio visible (bug):** posición de respawn desacoplada del mundo. (1) El backend respawnea correcto (`resolve_safe_spawn` → `player.position`, game_loop.rs:496-505) pero esa posición viaja por el `delta_update` que nadie consume → NUNCA se aplica al motor local. (2) El único respawn que mueve al jugador local es el de STP (`DeathUI.ResetHealth` → `SoloSurvivalGameMode.SetPlayerPosition` → `GetRandomSpawnPoint` → array `_spawnPoints[]`/fallback `transform`), transforms FIJOS de escena ajenos al mundo generado/streameado → aterriza en void/geometría/fuera de lo renderizado. Cruzado: muerte local solo dispara (2) con spawn inválido; muerte server-side (silenciosa) ni siquiera dispara la `DeathUI` (solo el fade cosmético de `PhantomAttackHandler`, sin reposición real).
- **Por qué juntos y con ADR-010:** arreglar P1 (daño autoritativo) y P2 (respawn autoritativo) correctamente exige construir el **consumidor de `delta_update`** que falta — exactamente la pieza que necesitaría el combate PvP real (ADR-010). Plan/ADR en sesión dedicada; toca red (sistema núcleo, reglas duras 4/7/9).

## Estado actual — [F] Item Held Sync (ADR-023) — IMPLEMENTADO (Slice 1+2), pendiente bake + play-test
- **Qué hace:** sincroniza el wieldable EN MANO del peer y lo muestra en su proxy 3P, adjunto al bone `Hand.R`. Cosmético/host-relay, NO autoritativo (no toca combate/hitreg). Extiende el camino de pose de ADR-011/020/021/022 y el relay de ADR-015.
- **Slice 1 (transporte + placeholder):** campo `held_item:i32` (`0`=manos vacías, `serde(default)`) por toda la superficie de pose (espejo de `equipment`): `PlayerInput`, `RemotePlayerState`, `PacketPayload::PlayerUpdate`, `PeerConnection`, `NetworkEvent`, `Player`, `broadcast_player_update`/`broadcast_peer_poses`, `build_world_state`. `WIRE_SCHEMA_VERSION` 5→6. `update_player_state` SIN tocar → robapieles con manos vacías gratis. `cargo test` 312/312 (round-trips con `held_item=12345`); release GNU recompilado + copiado a `Builds/Backend/` (`grep -aoc held_item exe`=2). Cliente TX: `PlayerPoseTransmitter` lee el id del holster (`WithTag(WieldableTag)` en `SelectedIndex`), lazy + re-resuelto en rig rebuild. Wire: `IPCClient.SendPlayerInput` header MsgPack 14→15 + `held_item`; `RemotePlayerMsg.heldItem`; `RemotePlayerView.heldItem` (reset en Acquire/Release).
- **Slice 2 (grip per-categoría, paramétrico):** la categoría es GRATIS desde el id vía `DataDefinition<ItemDefinition>.GetWithId(id).ParentGroup.Name` (de-prefijada: `Melee`/`Firearms`/`Tools` + fallback). `GripPoseSet` (ScriptableObject, `_Migration/STPIntegration/Resources/GripPoseSet.asset`, creado por el builder) define por categoría {offset/rot/escala del modelo bajo `Hand.R` + `fingerCurl`/`thumbCurl`} + `fingerBendAxis`/`invertBend` compartidos. SO (no campos del hook) para que la calibración PERSISTA durante Play. `ProxyHeldItemHook` cachea `Hand.R` + los 15 huesos de dedo derechos (`{Thumb,Index,Middle,Ring,Pinky}Finger.{1,2,3}.R`) con su bind pose; `Update` change-detect → instancia el mesh del `ItemPickup` neutralizado (sin Rigidbody/Collider/MonoBehaviours); `LateUpdate` reaplica colocación + curl (`bind*AngleAxis(curl·sign, axis)`), solo con item en mano. Builder `WireHeldItemHook` cablea la ref al SO (idempotente: respeta un asset existente). Compile-check Roslyn `BackroomsSurvival`→`Assembly-CSharp`→`Assembly-CSharp-Editor` los 3 en verde.
- **DEUDA VISUAL CONOCIDA (decisión 2026-06-30):** el grip se posa SOLO en `Hand.R`; items de 2 manos (Marlin 336, Wooden Bow, lanzas) dejan `Hand.L` flotando libre. Además el agarre paramétrico de **Firearms con 1 mano** tiene techo de calidad bajo → NO seguir afinando curls; REVISITAR con animaciones reales (clips retargeteados al esqueleto MaleSurvivor, mismo enfoque que crouch ADR-020) cuando haya capacidad. Paramétrico se queda para Melee/Tools (calidad aceptable). Ver memoria `f-held-item-slice2-grip`.
- **PENDIENTE:** (1) bake — menú *Backrooms ▸ Build Remote Avatar Prefab* (añade el hook a `RemotePlayerAvatar.prefab` + crea `GripPoseSet.asset`); no hecho (editor abierto). (2) play-test + calibración de Melee/Tools (curls en el SO en vivo; `fingerBendAxis`/signo es la incógnita del rig Generic; offset del modelo moviendo el hijo-instancia en Scene view). El commit de cierre NO incluye el bake del prefab.

## Estado actual — [E] Equipment/Clothing Sync (ADR-022) — COMPLETO (play-tested: la ropa de un peer se ve en su proxy)
- **CORRECCIÓN de un audit erróneo:** el `AUDIT.md` (§1 Equipment) afirmaba que STP **NO tiene** Wearable/ClothingSlot y que "todo el sistema de ropa hay que construirlo desde cero" — **FALSO**. STP trae un sistema de ropa LOCAL completo: `CharacterClothing` (`PolymindGames/STP/Code/Runtime/Utilities/CharacterClothing.cs`) + 4 contenedores de inventario tagueados `Head/Torso/Legs/Feet Equipment` (`ItemConstants`) + preview 3P (`CharacterPreviewUI`). El glob `*Clothing*` del audit debió encontrarlo. [E] REUTILIZA ese sistema, no lo reconstruye. (Nota de corrección anexada en AUDIT.md §1.)
- **Mecanismo visual de STP (auditado):** NO es mesh swap ni bone attachment. Cada item es un `SkinnedMeshRenderer` PRE-COLOCADO como hijo del modelo, bindeado al esqueleto; equipar = `CharacterClothing.SetClothing(BodyPoint, itemId)` hace `prevRenderer.SetActive(false)` + `newRenderer.SetActive(true)` + fija una opacity mask en el shader del cuerpo (`_OpacityMask_{bodyPoint}`). Lectura del item equipado por slot: `container.GetItemAtIndex(0).Item?.Id` (int; `Item.Id == DataIdReference._value`). Evento de cambio local: `IItemContainer.SlotChanged`.
- **Guardarropa AUDITADO del proxy** (`MTP_PlayerViewer.CharacterClothing._clothing`, base del que `RemotePlayerAvatar` es variante): **Head 2 · Torso 3 · Feet 1 · Legs 3 items · Hands vacío**. El proxy YA traía el guardarropa pre-colocado → [E] solo transporta los 4 IDs y llama `SetClothing`. INVARIANTE DE CONTENIDO: la ropa recogible del mundo debe estar ⊆ este guardarropa; un item fuera → slot vacío (degradación elegante, sin crash).
- **Wire (schema v4→v5):** `equipment:[i32;4]` (orden de protocolo `[Head, Torso, Legs, Feet]`, `0`=vacío; ≠ orden del enum `BodyPoint` Head/Torso/Feet/Hands/Legs) en `PlayerInput` (cliente→backend), `RemotePlayerState` (backend→Unity), `PacketPayload::PlayerUpdate` (P2P) — todos `serde(default)` → peer v4 interopera (`[0;4]`). Sellado: `player.equipment = received_input.equipment` (game_loop); `peer.equipment` se fija en `handle_packet` (rama PlayerUpdate), **NO** en `update_player_state` → el robapieles (ADR-016) queda con ropa default, gratis (mismo patrón que pitch). Relay vía `broadcast_player_update` + `broadcast_peer_poses` (ADR-015); `build_world_state` copia `peer.equipment`. `WIRE_SCHEMA_VERSION` 4→5. Round-trip test con `equipment=[101,202,303,404]`. 312/312 tests verdes; release GNU recompilado + copiado a `Builds/Backend/` (`grep -aoc equipment exe`=2).
- **Cliente TX:** `PlayerPoseTransmitter` LEE los 4 slots del `Inventory` local (`FindContainer(WithTag(...))`, contenedores cacheados y re-resueltos en rig rebuild; buffer `int[4]` sin alloc por envío) → `IPCClient.SendPlayerInput` (header MsgPack **13→14**, clave `equipment` array de 4; `WriteInt` codifica IDs negativos como int32). Rule #3: solo REPORTA (el apply local es de `CharacterClothing` nativo).
- **Cliente RX + proxy:** `RemotePlayerMsg`/`RemotePlayerView` += `equipment` (parse `IPCParse.IntArray`, normalizado a 4; reset a `[0;4]` en Acquire/Release). `ProxyClothingHook` (nuevo, espejo de `ProxyCrouchHook`) hace change-detect por slot (sentinela `int.MinValue` → primer valor siempre aplica, incl. `0`) → `SetClothing(BodyPoint, id)` solo en cambio; `OnEnable` re-arma para pool. Removable.
- **Invariante anti-NRE (clave, señalado como riesgo):** `RemoteAvatarPrefabBuilder.WireClothingHook` setea `CharacterClothing._attachToCharacter = false` → `Start()` retorna temprano, nunca llama `AttachToCharacter` sobre el inventario deshabilitado del proxy (un contenedor null haría NRE silencioso). `SetClothing` hace el swap sin tocar contenedores. Cableado idempotente + `RecordPrefabInstancePropertyModifications` (componente heredado del base) + bake del prefab.
- **Verificación:** backend `cargo test` 312/312 verde; cliente compile-check Roslyn en cadena (BRS → Assembly-CSharp → Assembly-CSharp-Editor) ALL GREEN; bake del prefab OK; play-test humano **OK a la primera**. Commiteado (cierre de [E]).

## Estado actual — [D] Foot IK Slice 1 (Body Grounding) — SUPERADO PARCIALMENTE 2026-07-06 (ver ▸ Pose Y real + grounding)
> **ACTUALIZACIÓN 2026-07-06:** este hook estuvo MUERTO desde su bake — buscaba el hueso "Hips", que no existe en este rig (es "Pelvis"; mismo hallazgo que ADR-028 Fase D hizo para el corpse). Corregido a `FindBone("Pelvis")` y COMPLETADO junto al fix de convención de pivote (`GridConstants.PlayerBaseY`) — verificado visualmente. El texto de abajo se conserva como diseño original; donde dice "Hips" léase "Pelvis". La Y del root ya no es `floor_player_y` aplanada sino la Y real del cliente (ADR-026 parcial), y el hook absorbe solo el residual pequeño.
- **Problema que resuelve:** el proxy se posiciona desde la pose del backend (`floor_player_y`), pero lo que se RENDERIZA y se pisa es el `ChunkStreamer` client-side (otro mundo, deuda conocida) → los pies del proxy flotan/se hunden en el suelo visible. El grounding raycastea al suelo renderizado y corrige.
- **Componente nuevo `Assets/_Migration/STPIntegration/RemoteAvatar/ProxyGroundingHook.cs`** (vive en Assembly-CSharp, sin asmdef en _Migration, igual que los demás hooks): en `LateUpdate` (tras el Animator) raycast hacia abajo desde `root.position + up*_rayUp` contra `GridChunkBuilder.GeoMask` (las Unity layers de suelo {0,14,15,16}, sin hardcodear); `gap = hit.point.y − root.position.y`; `Mathf.SmoothDamp` del offset; `Hips.position += up*offset` (mueve TODO el esqueleto → los pies aterrizan en el suelo VISIBLE). Rig GENERIC (no Humanoid, sin OnAnimatorIK): `Hips` cacheado POR NOMBRE en Awake (mismo patrón que `ProxyPitchHook` con Head/Neck), guard no-op si falta. `OnEnable` resetea el offset (reutilización de pool).
- **Airborne fade:** peso 1 mientras `|gap| ≤ _groundSnapMax` (0.5 m), desvanece linealmente a 0 en `_airborneFade` (1.5 m) → un salto (`ProxyJumpFeeder`), chunk displacement o respawn levantan el cuerpo lejos del suelo y NO lo pegan; en el ápex `|gap|` es máximo → suelta del todo. Sin hit → offset a 0.
- **Sin self-hit:** los `CapsuleCollider` del proxy están en layer 12, FUERA de `GeoMask` → el raycast nunca golpea el propio avatar; `QueryTriggerInteraction.Ignore` evita volúmenes trigger.
- **Cliente-only:** NO toca el `root` en red (toca `Hips`, hijo) → `RemotePlayerManager` (lerp de pose) y `ProxyLocomotionFeeder` (lee XZ del root) INTACTOS. Cero red, cero schema, cero ADR. Removible (borrar el archivo → el proxy vuelve a la Y cruda del backend).
- **Cableado:** `Assets/_Migration/STPIntegration/Editor/RemoteAvatarPrefabBuilder.cs` → `WireGroundingHook` (mirror idempotente de `WirePitchHook`) hornea defaults: `_rayUp=1`, `_rayDown=3`, `_groundSnapMax=0.5`, `_airborneFade=1.5`, `_smoothTime=0.1`. Doc-comment de la clase actualizado para listar el hook.
- **Alcance decidido (aprobado por el humano):** Slice 1 = Body Grounding (offset de Hips). Slice 2 = IK por pie de 2 huesos (UpperLeg/LowerLeg/Foot a la normal + caída de pelvis) queda como FOLLOW-UP si el play-test lo pide. Cadena de pierna confirmada del esqueleto MaleSurvivor: `Hips → UpperLeg.{L,R} → LowerLeg.{L,R} → Foot.{L,R} → Toes.{L,R}`.
- **Verificación:** compile-check Roslyn verde (csc .NET Core de Unity contra UnityEngine + `BackroomsSurvival.dll`; la ref a `GridChunkBuilder.GeoMask` resuelve). El edit del builder es mirror exacto de `WirePitchHook`. Bake del prefab (`RemotePlayerAvatar.prefab`, menú *Backrooms ▸ Build Remote Avatar Prefab*) y play-test PENDIENTES (editor abierto).

## Estado actual — Character Rotation Polish [C] (yaw del proxy) — IMPLEMENTADO (pendiente play-test de calibración)
- Cambio CLIENT-ONLY (sin red/schema/ADR), todo en `RemotePlayerManager`. El yaw del proxy pasó de `LerpAngle` exponencial a **`Mathf.SmoothDampAngle`** (críticamente amortiguado → menos lag en giros sostenidos, sin overshoot), con estado `RemotePlayerView.yawVelocity`.
- **Snap-guard:** si `|Mathf.DeltaAngle(currentY, targetRotation)| > yawSnapThreshold` (120° default) → snap directo + `yawVelocity=0` (respawn / chunk displacement / giro de 180° instantáneo no barren media vuelta).
- `rotationSmoothing` CONSERVADO como **deprecated** (tooltip; el campo ya no se usa pero se mantiene para no perder el valor serializado en escenas existentes). Nuevos `[SerializeField]`: `rotationSmoothTime=0.1` (seg-a-objetivo), `yawSnapThreshold=120`. `yawVelocity` se resetea en `Acquire` (spawn) y `Release`.
- Interacción con ADR-021: `ProxyPitchHook` lee `transform.right` en `LateUpdate` (tras el `Update` que fija el yaw) → yaw más suave = eje de pitch más suave. Compile-check Roslyn verde. Calibración (`rotationSmoothTime` ~0.08–0.12, `yawSnapThreshold` ~100–140°) en play-test.

## Estado actual — Camera Pitch Sync (ADR-021) — COMPLETO (play-tested: el proxy inclina la cabeza)
- **Decisión (2026-06-30):** propagar el pitch de cámara del peer como campo TIPADO `pitch:i8` (cuantizado a 1°, rango −90..90) en la pose relay. Cosmético/host-relay, NO autoritativo. ASIMETRÍA con crouch: el campo de ENTRADA ya existía (`PlayerInput.look[0]`, ADR-009 §8) → el cliente solo lo RELLENA (header MsgPack intacto en 13, sin off-by-one); lo NUEVO es solo el hop de salida (RemotePlayerState + P2P PlayerUpdate). Cuantización a i8 SOLO en el backend (el input sigue float; el byte viaja en el broadcast P×J ADR-015).
- **Backend (wire v3→v4):** `pitch:i8` recorre la misma superficie que `crouch` — `Player.pitch` (init 0), `game_loop` `player.pitch = quantize_pitch(received_input.look[0])` (clamp+round), `ipc::RemotePlayerState` (serde default), `protocol::PacketPayload::PlayerUpdate` (serde default, round-trip test con pitch=-45), `PeerConnection.pitch`, `NetworkEvent::RemotePlayerUpdate`, `sync::broadcast_player_update`/`broadcast_peer_poses`, `build_world_state`. `WIRE_SCHEMA_VERSION` 3→4 (ipc/server.rs). Ambos hops serializan con `rmp_serde::to_vec_named` (mapa por nombre) → la clave `pitch` viaja sola si el binario la tiene. `update_player_state` SIN tocar → el robapieles (ADR-016) mira al frente (pitch=0). 312/312 tests verdes; release GNU recompilado (sha b88badc2) + copiado a `Builds/Backend/`.
- **Cliente TX/RX:** `PlayerPoseTransmitter` LEE el pitch local de `ILookHandlerCC.ViewAngles.x` (resuelto del character vía `_motor.GetComponentInParent<ICharacter>().GetCC<ILookHandlerCC>()`; el motor es MonoBehaviour plano, no CharacterBehaviour) → lo pasa a `SendPlayerInput` (antes 0f). `RemotePlayerMsg.pitch` (int, `IPCParse.L`) + `RemotePlayerView.pitch` (float). El reader MsgPack decodifica i8 negativo a `long` (`0xd0/(sbyte)`), `ToLong` lo acepta → −63 parsea bien.
- **Proxy apply (`ProxyPitchHook`, nuevo):** en `LateUpdate` (tras el Animator), rotación ADITIVA en espacio-MUNDO alrededor de `transform.right` (robusto a rig Generic — sin `GetBoneTransform`). Bones cacheados por nombre (`Head`/`Neck`/`UpperSpine`/`MiddleSpine`), guard no-op si falta Head. Reparto Head 60% / Neck 40%; spine lean solo sobre umbral, cap ±30°, repartido en los 2 spines. `[SerializeField]`: `_lerpSpeed`, `_spineLeanThreshold`, `_maxSpineLean`, `_invertPitch`. Cableado en `RemoteAvatarPrefabBuilder.WirePitchHook` + bakeado en `RemotePlayerAvatar.prefab`. Removable. Vive en Assembly-CSharp (sin asmdef en _Migration).
- **Gotcha de despliegue (resuelto, ver memoria backend-build-deploy-gotchas):** `cargo test --release` NO relinkea `backrooms_server.exe` (los tests pasan con el código nuevo en el harness, pero el bin queda stale) → costó un ciclo de debug con `view.pitch=0`. SIEMPRE `cargo build --release` por separado para desplegar; verificar el campo en el binario con `grep -aoc pitch exe` (NO `strings`, no existe en este Git-Bash).

## Estado actual — Crouch System + Sync (ADR-020) — COMPLETO (play-tested: el proxy remoto se agacha)
- **Decisiones (2026-06-29):** crouch LOCAL = STP nativo (`CharacterCrouchState` + `IMotorCC.Height`/`CanSetHeight`/`HeightChanged`), NO se reimplementa; wire = campo TIPADO `crouch:bool` (no se reusa `animation` ni `move_state`), schema v2→v3; proxy = **clips reales en BlendTree 2D FreeformCartesian** (MovementSpeed×Crouched). Crouch es COSMÉTICO/host-relay, no autoritativo (no toca colisión/hitreg/stamina).
- **Backend (commit dcf3f1b):** `crouch:bool` recorre la superficie de `animation` — `ipc::PlayerInput` + `ipc::RemotePlayerState` (serde default), `protocol::PacketPayload::PlayerUpdate` (P2P, serde default → un peer v2 decodifica a false) + `network::PeerConnection` + `NetworkEvent::RemotePlayerUpdate` + `player::Player`, `sync::broadcast_player_update` / `broadcast_peer_poses` (relay ADR-015), `build_world_state`. `WIRE_SCHEMA_VERSION` 2→3 (ipc/server.rs). **`update_player_state` SIN tocar** (15 llamadores, incl. el fantasma) → `peer.crouch` se fija SOLO al recibir un `PlayerUpdate` real (handle_packet) ⇒ el robapieles nunca se agacha (crouch=false). 312/312 tests verdes (incl. `player_update_round_trip` con crouch); release GNU recompilado + copiado a `Builds/Backend/` (binario NO versionado en git).
- **Cliente:** `PlayerPoseTransmitter` LEE el crouch local del motor (`Height < DefaultHeight − 0.05`; STP lo APLICA, aquí solo se REPORTA — rule #3) → `IPCClient.SendPlayerInput(..., crouch)` (map MsgPack de **13** campos). `RemotePlayerMsg.crouch` + `RemotePlayerView.crouch`. `ProxyCrouchHook` conduce el param `Crouched` (0..1, lerp) del Animator desde `view.crouch` (lee la view igual que `ProxyPickupHook`); removable. Cableado en `RemoteAvatarPrefabBuilder.WireCrouchHook`.
- **Invariante (ADR-020):** `crouch` ortogonal a `animation` — nunca se pisan (pickup-estando-agachado: ambos campos viajan; el proxy reproduce pickup full-body Y mantiene el offset de crouch). Receptor v2 sin `crouch` → false (de pie), sin error. No muta estado de juego.
- **Proxy visual (clips reales):** FBX `CrouchIdle`/`CrouchedWalking` (Generic + esqueleto MaleSurvivor — MISMO formato que walk/run, NO retarget Humanoid) en BlendTree 2D **FreeformCartesian** (X=`MovementSpeed`, Y=`Crouched`): standing idle/walk/run @Y0, crouch idle/walk @Y1 (crouch-run reusa crouch-walk). `ProxyAnimatorControllerBuilder.AddMovementState` crea el param `Crouched`. Pickup/Jump siguen Any-State full-body → tapan un instante y vuelven al blend AÚN agachado ⇒ pickup+crouch conviven.
- **BUG 1 (T-pose SOLO en build) — FIX:** la ref al controller (override de variant, fuera de Resources) no sobrevivía la build → el Animator caía al `AnimatorOverrideController` vendor (walk/run vacíos = T-pose). `ProxyControllerBinder` (nuevo) tiene una **referencia serializada** al controller (dependencia de build dura, nunca stripped) y lo **bindea en Awake** (`[DefaultExecutionOrder(-100)]`), independiente del override. Cableado en `RemoteAvatarPrefabBuilder.WireControllerBinder`. (Editor Play ya iba por el override; esto añade la garantía de build.)
- **BUG 2 (crouch no llegaba al backend) — FIX:** `IPCClient.SendPlayerInput` declaraba `WriteMapHeader(12)` pero escribía **13** pares → `rmp_serde` leía 12 y descartaba el 13º (`crouch`) → `PlayerInput.crouch`=false (serde default). Fix: header → **13**. (Off-by-one latente: antes 11 con 12 claves, `buttons` se descartaba inofensivo.) Aislado con logs temporales de 4 hops backend (RX_INPUT/TX_P2P/RX_PEER/WS) + 2 C#, ya retirados. DEUDA menor: el `WriteMapHeader(n)` manual es frágil — auto-contar los pares evitaría recaer.

## En curso / a medias
- Migración STP servidor-autoritativo: Steps 1–2 + Step 3 slice 3.1 (plumbing de protocolo) DONE y verificados. Falta slice 3.2 (capa L2 de predicción) y la reescritura de los 8 call sites de Inventory.
- PlayerController.cs (proyecto Backrooms) marcado DEPRECATED por ADR-009 — pendiente de borrar/stub en slice 3.2 (conflicto con autoridad de movimiento de STP).
- Fase 2 (retraso desaparición item + reserva anti-duplicado): backend IMPLEMENTADO (ADR-014 — pending_pickups host-only, borrado diferido PICKUP_REMOVE_DELAY=200ms, drenado por tick, purga en set_stp_items, rechazo de reservados). Pendiente play-test.

## Estado actual — Robapieles ADR-016 — base + identidad/tell (IMPLEMENTADO backend, pendiente play-test)
- Identidad de víctima (NUEVO): el fantasma clona el NOMBRE de un peer real (víctima) manteniendo su ID propio 0xF000 (el ID distinto = tell #1). `choose_victim_name` (primer peer real; fallback = nombre del host en solo) al spawnear + `PhantomDriver::rebind_unbound_victims` re-vincula una vez cuando conecta un peer real (escribe `PeerConnection.name` → propaga por roster/PeerList + ADR-015, sin schema). INVARIANTE: solo el NOMBRE, NUNCA el ID. Validado runtime (host + joiner "Joel" → fantasma adopta "Joel", phantom_id=61440). Test `phantom_clones_victim_name_but_keeps_its_own_id`.
- Tell comportamental #2 (NUEVO): "stare" — inmovilidad antinatural en cadencia casi metronómica (`PHANTOM_STARE_INTERVAL`=30s / `PHANTOM_STARE_DURATION`=2.5s), pose congelada + "idle". Sutil pero aprendible; PURAMENTE comportamental (sin flag en el wire). Calibrable. Traza PH6. (tell #1 = ID raro.)
- Limpieza de scaffolding (NUEVO): `DEBUG_SPAWN_PHANTOM` ya NO es const hardcoded `true` → ahora env flag `env_flag_enabled("DEBUG_SPAWN_PHANTOM")`, OFF por defecto (ninguna build normal auto-spawnea). Sigue siendo el disparador de debug (set en el entorno para probar). No había scaffolding C# (`[PICKUP_DEBUG]`/`PickupDebugStateLogger` no existen). MPTRACE PH2/PH4/PH5/PH6 permanentes (diagnóstico) — se quedan.
- Slice 4 = PICKUP IMITADO (puro teatro): el `PhantomDriver` cada `PHANTOM_PICKUP_INTERVAL`=6s fija `animation="pickup"` en el `PeerConnection` del fantasma durante `PHANTOM_PICKUP_GESTURE`=1s (ventana de flanco ADR-011) y PAUSA el movimiento (imita el input-lock del jugador real, sin usarlo), luego vuelve a "idle" y reanuda. **INVARIANTE DE SEGURIDAD (verificada): toca SOLO el campo `animation` (vía `update_player_state`) — NUNCA `process_stp_pickup`, NUNCA `pending_pickups`, NUNCA `stp_items`, NUNCA grant/inventario. Cero efecto sobre el estado real del juego.** Se propaga a joiners vía ADR-015 (mismo relay de pose que loc/rot). Test `phantom_fake_pickup_touches_only_animation_not_real_state` (item sembrado sobrevive, pending_pickups/grants vacíos). Smoke: PH4 cada 6s, sin ningún `step=SP`/grant; el fantasma reanuda el walk tras el gesto.
- Slice 2 = MOVIMIENTO con colisión sim-only (primera prueba runtime de ADR-017). Driver host-only `PhantomDriver` (game_loop.rs): cada fantasma camina por su heading; cada paso se resuelve con `Level0Collision::resolve_move_simulated(world, &mut SimChunkCache, from, desired)` (ADR-017) → respeta paredes/suelo incluso lejos del host (donde world.chunks está vacío, la caché genera el layout on-demand). En bloqueo de frente re-orienta el heading (giro aleatorio 90°–270°) → nunca se atasca; al deslizar (SlidX/SlidZ) mantiene heading y bordea el muro. Rotación = el heading (mira hacia donde anda, propagado a joiners vía ADR-015). Suelo: usa la Y de `CollisionResolve.position` (floor_player_y_src) → deja de flotar. Velocidad `PHANTOM_WALK_SPEED=1.8 m/s` (calibrable; dispara locomoción derivada ADR-013 sin tripear el guard de teleport). `animation="idle"` (locomoción es velocity-derived; el canal animation se reserva a acciones/pickup, slice 4). La `SimChunkCache` vive en el `PhantomDriver` (local host-only del game loop), NO en World/NetworkManager (menos invasivo). Driver corre a 10Hz (cadencia entity-tick).
- Smoke runtime: el fantasma camina (x 25.5→69.6 en 28s ≈1.6 m/s), grounded y=1.80, yaw=90 (mira +X), y al topar geometría hace SlidZ (desliza, NO cruza). Test unitario `phantom_driver_walks_via_sim_cache_far_from_host` prueba el path sim-only far (sim cache generada, pose grounded) determinísticamente.
- **LIMITACIÓN (deuda, hallazgo 2026-06-19):** la colisión del robapieles es contra el mundo del BACKEND, que NO coincide geométricamente con el `ChunkStreamer` client-side que se RENDERIZA → el robapieles cruza/esquiva paredes incorrectamente respecto a lo que se ve. Es un PROTOTIPO con colisión imperfecta hasta la migración del mundo a backend-authoritative. Detalle en ▸ Deuda conocida; resolución en ▸ Riesgos abiertos. (Los slices restantes —pickup imitado— NO dependen de la colisión.)
- Slice 1 = peer fantasma ESTÁTICO: aparece en el roster, parado, renderizado como un jugador (nameplate), indistinguible de un peer real. Sin IA de movimiento, sin pickup.
- Marca interna: `NetworkManager.phantom_ids: HashSet<PeerId>` (host-only, backend-only). NUNCA viaja por el wire (no en `PeerInfo` ni `RemotePlayerState`) → un joiner no puede distinguirlo de un peer real (su propio `phantom_ids` queda vacío).
- Inyección: `NetworkManager::spawn_phantom(name, pos)` inserta directo en `net.peers` FUERA del handshake (sin `PeerConnected` → sin `send_world_sync`). Id en rango dedicado ≥ `0xF000` (sin colisión con host=1 / fallbacks desde 2 / NET_ID joiners ∈ [1000,60999]). addr inerte 127.0.0.1:1.
- Heartbeat: `refresh_phantom_heartbeats()` llamado en el heartbeat-tick (1s) antes de `check_timeouts` → el fantasma no se reapea a los 5s.
- Higiene de conteo: `real_peer_count()` (excluye fantasmas) reemplaza `peer_count()` en los dos gates internos (sanity `stat_context_for`, spawn de joiner `>0`). El roster (`build_world_state`) sigue incluyendo al fantasma.
- Higiene de reliables: `broadcast_reliable` (anchor/stabilizer) y `broadcast_world_sync` saltan al fantasma (addr inerte). `send_reliable` dirigido intacto (nunca apunta a un fantasma).
- Disparador de PRUEBA (slice 1): `const DEBUG_SPAWN_PHANTOM = true` en game_loop.rs → auto-spawn de un fantasma "Robapieles_Test" a 3 m del spawn del host al arrancar. TEMPORAL (en slices futuros lo dispara la lógica de juego); poner a `false` tras validar.
- Verificado (slices 1+2+4 + identidad/tell): `cargo build --release` (GNU) verde; suite 289/289 verde (7 tests de fantasma: roster/real_count, timeout-refresh, reliable-skip, driver sim-cache far, fake-pickup-invariant, victim-name-clone); `backrooms_server.exe` recompilado y copiado a Builds/Backend/. Smoke runtime: cleanup (sin flag→no spawn), PH5 victim=Joel, PH4 pickups, PH6 stare a ~30s, sin pickup real. NO commiteado (pendiente ver en escena).

## Estado actual — Sistema de animación de proxies (DONE 2026-06-18)
- **Locomoción:** `RemoteAvatar/ProxyLocomotionFeeder.cs` — escribe `MovementSpeed` derivado de la velocidad planar (delta de transform.position interpolado por RemotePlayerManager / dt), mapeo a tiers 0/1/3 (Idle/Walk/Run) con deadzone+SmoothDamp y guard de teleport XZ. NO toca red.
- **Jump:** `RemoteAvatar/ProxyJumpFeeder.cs` — derivado de velocidad vertical (deltaY/dt) client-side, edge-detection con latch, guard de teleport VERTICAL (chunk displacement/respawn no disparan falso jump) y discriminación rampa/escalera (umbral jumpVelocityUp por encima de la Y sostenida de rampa). Dispara trigger "Jump". NO toca red.
- **Pickup:** FULL-BODY en Base Layer (NO upper-body: la AvatarMask amputaba el gesto que flexiona piernas para recoger del suelo). Señal vía campo `animation:String` desde backend (ADR-011). `RemoteAvatar/ProxyPickupHook.cs` con edge-detection sobre `rp.animation=="pickup"` (latch que distingue null/desconocido de no-pickup → fix del doble-trigger). Estado Pickup con Speed=2 (gesto a ~0.58s). Input lock LOCAL durante el gesto: `RemoteAvatar/LocalPickupInputLock.cs` (self-bootstrap) engancha el evento IPC `stp_pickup_granted` (confirmación local) y usa el `AddStateBlocker` nativo de STP (bloquea Walk/Run/Jump; el motor sigue vivo → PlayerPoseTransmitter sigue emitiendo la pose quieta). Duración del lock horneada del clip (clip.length/PickupSpeed) en `ProxyPickupHook.GestureDuration`, leída del prefab en Resources — NO hardcodeada.
- **Controller:** `RemoteAvatar/ProxyLocomotionController.controller` (custom, reemplaza el AnimatorOverrideController vendor STP_MaleSurvivor), generado por `Editor/ProxyAnimatorControllerBuilder.cs`. Base Layer: Movement (BlendTree 1D-Simple, MovementSpeed, idle@0/walk@1/run@3) + Jump + Pickup, los tres FULL-BODY. Una sola capa, sin UpperBody, sin máscara.
- **Binding durable:** `Editor/RemoteAvatarPrefabBuilder.cs` (menú "Backrooms ▸ Build Remote Avatar Prefab") asigna el controller custom a la variante y lo PERSISTE (RecordPrefabInstancePropertyModifications + re-binding sobre el asset guardado vía EnsureControllerBound). Resuelto el bug por el que el prefab corría el override vendor (walk/run vacíos → T-pose). El builder también cablea, idempotente y durable, los 3 feeders/hook (Wire* mirrors) con sus valores por defecto.
- **Backend (ADR-011):** sello `last_pickup_at` en `NetworkManager` (backend/src/network/mod.rs), sellado en `process_stp_pickup` rama local Y en la rama joiner `StpPickupGranted` (game_loop.rs), y prioridad `animation="pickup"` durante ~1s en `broadcast_player_update` (sync.rs). Sin campo nuevo de schema. `backrooms_server.exe` recompilado (toolchain GNU) y copiado a `Builds/Backend/`.

## Estado actual — Integración STP servidor-autoritativo (Steps 1–2 DONE)
- Decisión de arquitectura: STP es la capa cliente y conduce la experiencia; Rust posee TODO el estado autoritativo. PlayerController.cs (Backrooms) DEPRECATED.
- Step 1 (Audit): 18 MonoBehaviours con estado server-authoritative; ~30 métodos que escriben estado (candidatos a request). 5 fricciones top: Inventory.Add/Remove (8 call sites), drenaje de stats en Update(), conflicto PlayerMovementController↔PlayerController, ISaveableComponent en 6 clases, CraftingManager.OnCraftItemEnd acoplado.
- Step 2 (Capas): L0 Server / L1 Network (IPCClient, existe) / L2 Sync (NEW: MovementReconciler 20Hz, StatInterpolator 5Hz, WorldStateApplier 5Hz, TimeSync 1Hz) / L3 STP Gameplay (predicho/display) / L4 Presentation (local-only). L2 es la ÚNICA capa que escribe en componentes STP. Contratos declarados (sin reescribir call sites todavía).
- ADR-009 (VALIDADA 2026-06-13, §5 ENMENDADA 2026-06-14): protocolo player/stat/world + predicción cliente. Predicción (§5 enmendada): ring buffer 8 ticks/400 ms solo para detectar desync; corrección posicional delta/3 sobre 3 frames vía CharacterController.Move + velocidad inmediata; SIN rollback/replay (estado interno de CharacterController no snapshotable → replay no determinista). Umbral reconciliación 0.15 m, look=input (sin sink L2), sanity 5Hz→SanityEffectsController. Comparte transporte con ADR-005. Dependencia ADR-003 (asume servidor único; si distribuido, revisar ackInputSeq).
- Checklist backend A–E (prereq Step 3): A=schema ipc/mod.rs (extender PlayerInput, +stamina en StatsView, +ack_input_seq); B=game_loop.rs (guardar input_seq, apply_movement Opción B = validar pose cliente + clamp, NO integración física server-side; tick rates 10Hz→20/5/5/1); C=player/stats.rs (+stamina, drenaje sanity 5Hz); D=server.rs (sin cambios funcionales, es transporte; opcional versión de schema en handshake); E=bump schema + build/tests verdes. server.rs queda casi intacto.
- apply_movement = Opción B (decidida): cliente envía position+velocity; servidor valida (speed cap: rechaza si |velocity| > maxSpeed + 0.5 m/s tolerancia; colisión: verifica que la posición no intersecta geometría estática) y devuelve pose autoritativa (clamp si necesario) con ackInputSeq. Confía en la predicción cliente, recorta outliers.
- ADR-009: VALIDADA (2026-06-13). Slice 3.1 (plumbing) DONE: A=ipc/mod.rs (PlayerInput +input_seq/client_tick/position/velocity/move_state/look/buttons serde(default); StatsView +stamina; LocalPlayerState +ack_input_seq). C=stats.rs (PlayerStats +stamina, regen 8/s + use_stamina, drain run 15/s). C#=IPCClient.SendPlayerInput + StatsMsg.stamina + LocalPlayerMsg.ackInputSeq. D=server.rs WIRE_SCHEMA_VERSION=2 (campos serde(default) → v1 interopera). Verificado: cargo check + tests ipc (4/4) verdes; Roslyn C# verde.
- B=game_loop.rs (refactor pre-3.2, 2026-06-14): apply_movement→Option<u32> envuelve apply_client_authoritative_move (Opción B); gate input_seq!=0 ELIMINADO (input_seq ahora 0-based) y path legacy de integración por dirección BORRADO. last_input→received_input + has_received_input; last_accepted_input_seq:u32 (init u32::MAX = "nada aceptado aún"). El ack del snapshot = último input ACEPTADO (None en rechazo por speed cap, no avanza el ack). apply_movement solo corre con has_received_input (evita arrastrar al origen con el PlayerInput default pos=[0,0,0]). CollisionResultKind retirado del import (solo lo usaba el path legacy).
- Slice 3.2 — stream delta 20 Hz (Rust) DONE (2026-06-14): ipc/mod.rs ServerMessage::DeltaUpdate(MovementDelta{tick, ack_input_seq, position, velocity}); tag wire "delta_update". game_loop.rs: MOVEMENT_DELTA_EVERY=3 (20 Hz, 60/3), emite el delta antes del WorldState; trackea authoritative_velocity (= received_input.velocity si aceptado, Vec3::ZERO si rechazado por speed cap). El C# IPCClient ignora "delta_update" sin error (switch sin default) hasta que el reconciler lo consuma. Verificado: cargo test ipc 5/5 (incl. movement_delta_round_trips).
- ADR-010 (PROPUESTA, 2026-06-14): hitreg event-driven con compensación de lag (event_loop fuera del game loop 20 Hz; PositionHistory VecDeque<(u64,Vec3)>; ShootMessage). Sistema FUTURO; bloqueado por diseño de combate; no bloquea nada del roadmap. (Actualización 2026-07-06: el combate PvP V0 se implementó vía ADR-029 SIN lag compensation — el hitreg con rewind de este ADR sigue siendo el objetivo futuro si el playtest lo exige.)
- Slice 3.2 — MovementReconciler (C#) DONE (2026-06-14): (1) IPCMessages.cs MovementDeltaMsg + IPCClient case "delta_update" → AddMovementDeltaListener (drenado en Update main-thread). (2) MovementReconciler.cs [DefaultExecutionOrder(100)] (corre tras el motor): envía PlayerInput a 20 Hz con inputSeq 0-based (el reconciler ES el sender), snapshot en ring buffer de 8; on delta busca por ackInputSeq, si |error|>0.15 m arranca corrección posicional error/N por N frames vía motor.Move y motor.SetVelocity inmediata. ackInputSeq==uint.MaxValue → skip. move_state mapeado STP→ADR (STP Walk=2/Run=3 ≠ ADR run=2). (3) CharacterControllerMotor: +Move(Vector3)→CollisionFlags y +SetVelocity(v) (fija _simulatedVelocity+_velocity+_lastPosition=pos, anti-spike); Update() intacto. (4) BackroomsSurvival.asmdef +ref "PolymindGames" (acíclico). Verificado Roslyn: PolymindGames fresco (con edición del motor) 624 src 0 err → BackroomsSurvival 0 err. NOTA metodológica: el csproj correcto es BackroomsSurvival (no Assembly-CSharp); con ediciones a PolymindGames sin recompilar Unity, hay que overridear el DLL stale con uno csc'd fresco (CS0433 si coexisten).
- DECISION (autonomous): MovementReconciler is the sender of PlayerInputMessage at 20Hz. It observes the realized pose from CharacterControllerMotor after each Update() and reports it. PlayerMovementController owns prediction; Reconciler owns reporting.
- Slice 3.2 — StatInterpolator (C#) DONE (2026-06-14): StatInterpolator.cs (BackroomsSurvival.Gameplay) con 4 binders anidados (StatBinder base de buffer 2-muestras: render en now−50 ms, Clamp01 → sin overshoot; mantiene último valor si para la señal). Hunger/Thirst binders: TakeControl→enabled=false + suscriben HealthManager.Respawn → OnRespawn hace SnapTo(max) (jump duro, no SetTarget que rampearía); ReleaseControl→enabled=true + desuscribe. Stamina binder: SetServerControlled(true) (skip drain en Update, conserva setter→StaminaChanged+blocking+audio); escala server/100 (STP stamina es 0..1, server 0..100). Health binder: SetHealthSilent (sin DamageReceived/HealthRestored); Take/Release no-op (health no drena). Toma control con _ipc.IsConnected, libera al desconectar (fallback a drain local). Suscribe AddStateListener → lee state.localPlayer.stats (5 Hz). Sanity FUERA (va a SanityEffectsController). Ediciones STP: StaminaManager +_serverControlled +SetServerControlled(bool) +guard en Update; HealthManager +SetHealthSilent(float). Hooks PÚBLICOS (no internal — cross-assembly BackroomsSurvival↔PolymindGames). Verificado Roslyn: PolymindGames fresco + BackroomsSurvival 0 err.
- Respawn (RESUELTO 2026-06-14): PlayerStats::on_respawn ahora hunger/thirst=100 (antes 50), casando con el SnapTo(max=100) del binder → respawn converge a full en vez de caer a 50. sanity sigue en 50. cargo check verde.
- Connection gate DONE (2026-06-14): GameMode.Start() espera conexión antes de spawnear el player. NO se referencia IPCClient directo (sería ciclo de assemblies: GameMode∈PolymindGames, IPCClient∈BackroomsSurvival que ya referencia PolymindGames). Solución desacoplada: GameBootGate.cs (nuevo, PolymindGames) = static Func<bool> IsReady, default ()=>true; GameMode hace polling sobre GameBootGate.IsReady() con timeout 10 s (offline fallback), preservando el yield original. GameBootGateBinder.cs (nuevo, BackroomsSurvival) = MonoBehaviour scene-scoped que en Awake asigna IsReady=()=>IPCClient conectado y en OnDestroy restaura ()=>true. ATAR a la escena BackroomsWithSTP (no a escenas STP standalone, que conservan default always-ready → spawn inmediato). Verificado Roslyn: PolymindGames + BackroomsSurvival 0 err. (Intento previo de usar IPCClient directo en GameMode falló con CS0103 por el ciclo — confirmado.)
- Slice 3.2 — ISaveableComponent no-op DONE (2026-06-14): las 6 clases (HealthManager, Inventory, CraftingManager, CharacterControllerMotor, PlayerMovementController, CharacterLookHandler) → LoadMembers(){} vacío, SaveMembers()=>null; clases SaveData privadas muertas eliminadas; interfaz y firmas (explícitas vs públicas) intactas. Save STP deshabilitado, Rust única fuente de verdad. NOTA: StaminaManager también implementa ISaveableComponent pero NO estaba en la lista de 6 (su save sigue activo; afinar si se quiere). Verificado: cargo check verde; Roslyn PolymindGames fresco + BackroomsSurvival 0 err.
- Pendiente: slice 3.2 — MovementReconciler/StatInterpolator/TimeSync en C# + borrar PlayerController.cs. Nota: el reconciler debe tratar ack_input_seq==u32::MAX (uint.MaxValue en C#) como "sin ack todavía".

## Estado actual — Sistema de aristas edge-based (DONE)
- Edge-based walls: DONE (capa Unity-only, sin ADR; contrato Rust intacto).
- Todos los tiles tienen suelo y techo garantizado (cero huecos).
- Paredes = paneles 0.2 m en los bordes ENTRE tiles (no bloques). Builder emite solo aristas N/E (regla de no-duplicar: cada arista física se renderiza una vez); S/W las emite el tile/chunk vecino.
- GenerateChunk devuelve ChunkData{cells,walls}; cells salen Corridor/Pillar (sin celdas Wall). Conectividad por BFS + aperturas de seam fijas {1,5,9}.
- ADR-008 (Pit→Hollow) preservado: rama Hollow viva, pero el generador no emite pits.
- Verticalidad: PENDIENTE — se hará vía superestructuras (StructureDefinition sigue como esquema de autoría, sin consumidor en este generador).
- Verificado: build + EditMode tests en verde (Roslyn headless). NO ejecutado en Play (pendiente validación visual: maze sin huecos, paneles finos, seams alineadas).

## Estado actual — Sistema de Tiles 5m (DONE)
- Render migrado a tiles de 5×5 m (2×2 celdas Rust)
- Wall: 5×4×0.2 m, piezas independientes (LEGO), altura uniforme 4 m
- Floor/Ceiling: prefabs separados 5×0.2×5 m, ceiling fijo a 4.04 m
- Pillar 4 m, VoidEdge 5 m
- WallGreedyMesher: DEPRECATED (referencia hasta Fase 5)
- Eliminados: FloorCeiling, Stair, CeilingStep, fascias
- Verificado en Play (a 15 m de capa): sin huecos, sin z-fighting. LayerHeight ahora 4 m por ADR-007 — sin re-Play a esa altura.

## ADR-007 — implementado parcial
- LayerHeight = 4 m en Rust+Unity ✓
- LayerRules serializable + 4 campos nuevos ✓
- JSON defaults en StreamingAssets ✓
- Cableado wall_density/inter_layer al algoritmo: PENDIENTE
- load_profiles no conectado end-to-end hasta ADR-005 IPC

## NO tocar (validado 2026-07-03)
> Cambios en estos sistemas requieren re-validación end-to-end en runtime.
- **Cadena de respawn Y muerte:** RespawnRequester (click DeathUI + `ForceNativeDeathEdge` para muerte server-side silenciosa + neutralización del auto-respawn de 15 s vía StopAllCoroutines + `ForceNativeRespawnChain`) + AuthoritativePoseApplier (snap pos) + gate SnapPending en PlayerPoseTransmitter (no re-afirma pose vieja). Dependencias cruzadas; cambios sin re-test causarán rubber-banding/aterrizado inválido/jugador varado muerto.
- **Gate volumetric near-spawn:** backend `volumetric_grid` contrato "ONLY en chunk near-spawn showcase". Disyuntivo chunk_is_v30a retirado del gate IPC; mantener deshabilitado (ENABLE_LEVEL0_VOLUMETRIC_COLUMNS=false en módulo).
- **Puente UI de salud (SilentHealthUIBridge):** sincronización fillAmount vía reflexión. Cambios en HealthUI/Health deben conservar compatibilidad de nombres.

## Deuda conocida
- (2026-07-28) **Opción C del cableado de `zone_kind` — llevar `zone_kind` dentro de `GridChunkData` (respuesta a `RequestChunk`) — REQUIERE ADR, no implementada.** Es la solución correcta a largo plazo para la carrera de mensajes descrita abajo: hoy la geometría (`ChunkData`, derivada de grid_gen) y `zone_kind` (que solo viaja en `ChunkView` dentro de `WorldStateMsg`) son mensajes IPC INDEPENDIENTES sin orden garantizado, así que el cliente construye chunks antes de conocer su zona. Llevando el campo en el propio `GridChunkData` la zona llegaría ATÓMICAMENTE con la geometría y la carrera desaparecería de raíz (sin gates, sin timeouts, sin reintentos). Coste: es cambio de protocolo (regla dura #7 → ADR nuevo obligatorio) y toca el handler de `RequestChunk` en `game_loop.rs` (~:426-436, sistema núcleo), que necesitaría un lookup `&mut World` (`ensure_chunk_layer(...).layout.zone_kind`) para leer el dato. Mitigado por ahora en cliente (ver nota de trabajo en curso abajo); retomar cuando se quiera eliminar la dependencia temporal por completo.
- (2026-07-28) **Hallazgo lateral del sistema legacy: `tick_teleportation` deja `zone_kind` desincronizado del template tras un chunk displacement.** En `backend/src/world/mod.rs` (~:888-894) el displacement reasigna `chunk.template_id`, `rotation`, `mirrored`, `entities` e `items` desde `generate_chunk(new_seed, old_pos)`, pero **NO** reasigna `chunk.layout` — así que `layout.zone_kind` (y el resto del layout legacy: cells, edges) conserva el valor del template ANTERIOR. Consecuencia: tras desplazarse, un chunk reporta una zona que ya no corresponde a su `template_id`. **No afecta al render hoy** porque la geometría que Unity dibuja viene de grid_gen (`chunk_tile_walls`, derivada de world_seed+coords), no de `chunk.layout`; el efecto visible se limita a la clasificación semántica. Efecto colateral útil y verificado: `zone_kind` es por tanto ESTABLE por `(cx,cz)` durante toda la sesión desde el punto de vista del cliente — por eso el cableado de estética no necesita re-tintado continuo. Deuda preexistente del sistema legacy, no tocada.
- (2026-07-28) Cableado `zone_kind` → estética: COMPLETO y verificado en playtest — ver ▸ Estado actual (zone_kind → estética). Pendiente solo la Pieza 3 (spawns/loot por zona).
- (2026-07-07) **`TODO(balance)` acumulados del bloque de loot/consumo** (ninguno bloqueante, todos placeholder razonables confirmados jugables en playtest): (a) valores de restauración de ADR-030 (`consumable_spec` — punto medio de los rangos autorados, simplificación rango→fijo pendiente de pasada de balance); (b) cantidad/composición de loot: 6 caches × 6 items, 4 zonas × 8 carryables, 4 cofres con ~8-9 stacks (arma garantizada + munición 5-10 + médicos + consumibles + materiales); (c) `max_damage`/`max_range` de la allowlist PvP (ya anotado en ▸ ADR-029); (d) `MAX_CLAIMED_Y_STEP=3.0` de ADR-026 parcial. Una sola sesión de balance futura puede barrerlos todos.
- (2026-07-07) ADR-030: deuda de confianza — sin anti-cheat de posesión/cantidad en `consume_item` (un cliente modificado restauraría stats gratis); mismo nivel trust-the-client ya aceptado en pickup/death-loot/report_damage hasta que exista inventario autoritativo (documentado en el ADR).
- (2026-07-06) **Ediciones de instrumentación MPTRACE en archivos vendor STP sin commitear y SIN DECISIÓN** (`Assets/PolymindGames/.../FirearmHitscanFireSystem.cs`, `FirearmBasicImpactEffect.cs`, `FirearmStandardImpactEffect.cs`, `BasicMeleeAttack.cs` — solo logs de diagnóstico PvP, añadidos durante ADR-029 Fase 2): pendiente decidir si se conservan o se revierten. NO tocar código hasta decidirlo (regla "nunca editar STP" ya violada solo con logs — regularizar en la decisión).
- (2026-07-06) Deuda PvP V0 (detallada en ▸ Estado actual ADR-029): `victim_invulnerable` no verificable host-side para peers remotos (sin agujero — la víctima re-valida; sin feedback al shooter); allowlist con valores `TODO(balance)`; Fase 5 (hitmarker) no implementada.
- (ADR-025, 2026-07-03) **Verticalidad pisable perdida tras el fix del payload `volumetric_grid`** (mitad A, aparcada SIN decisión): el camino de fall damage está INTACTO (probado con caída instrumentada: `report_damage_applied cause=Fall`), pero al restaurar el contrato near-spawn las columnas volumétricas dejaron de renderizarse y, con movimiento client-authoritative, lo renderizado ES lo colisionable → no queda de dónde caerse en juego normal. Opciones cuando se decida: columnas en radio cercano al jugador vs. aceptar mundo plano hasta la migración de render de worldgraph. NO bloqueante.
- (2026-07-03) **F-keys sintéticos no llegan al juego en editor** (solo pulsaciones físicas; los atajos de editor Ctrl+P/Ctrl+R sí funcionan por SendKeys): para tests automatizados la vía fiable es la inyección sidecar por IPC TCP (msgpack, `{'type':'action',…}`) contra el backend vivo — detalle en memoria del proyecto (`unity-remote-playtest`).
- (2026-07-03) **Riesgo: ediciones cruzadas de Claude en el mismo árbol**: una sesión editó `.plastic/plastic.wktree` + STATE.md + DECISIONS.md; otra sesión concurrente generó tmp huérfano en Temp/. Política: UNA sesión por working tree mientras haya cambios sin pulir.
- [D] grounding: `GeoMask` incluye la layer 0 (Default) → un prop con collider directamente bajo los pies podría capturar el raycast en vez del slab de suelo; aceptable en slice 1, revisar en play-test.
- [D] grounding: el airborne fade discrimina por `|gap|`; un salto de altura menor que `_airborneFade` podría recibir un tirón leve hacia abajo. Calibrar `_groundSnapMax`/`_airborneFade` vs. la altura real del salto y el desfase real entre mundos en play-test.
- (2026-07-28) **Hallazgo ADR-018 (desajuste geometría 2.5m/5m) — investigado, NO tocado, documentado para cuando se retome ADR-026:**
  - `resolve_move_grid_gen`/`resolve_spawn_near`/`is_walkable_grid_gen` (`backend/src/world/grid_gen/collision.rs`) consultan la celda cruda de 2.5m en vez del bitmask de 5m (`tile_walls_from_grid`) — confirmado por grep de TODOS los call sites: este camino es EXCLUSIVO del fantasma/robapieles (`game_loop.rs` dentro de `PHANTOM_WANDER`, y `network/mod.rs::spawn_phantom`). El jugador real NUNCA pasa por grid_gen para colisión.
  - El jugador real colisiona contra `Level0Collision::resolve_move`/`resolve_safe_spawn` (`world/collision.rs`), que leen `ChunkLayoutV1` de `world::generator` — un generador de mundo TOTALMENTE DISTINTO y desconectado de grid_gen/tile_walls (lo que Unity renderiza). Este es el desajuste que de verdad afecta la sensación de mundo, y coincide exactamente con ADR-026 partes 1-2 (ya BLOQUEADAS pendiente del diagnóstico TP_WATCH) — no se tocó en esta sesión.
  - Contrato del bitmask de 5m (para no re-derivarlo cuando se retome): `tile_walls_from_grid` codifica bloqueo por ARISTA direccional independiente (4 bits N/S/E/W por tile, `tile_walls.rs`), NUNCA ocupación de bloque — el wire `GridChunkData` (`ipc/mod.rs`) solo transporta `cx/cz/walls`, sin campo de floor/void por tile, así que la distinción Void/Pit/Anomaly se pierde en la cuantización 2.5m→5m tal como está el contrato hoy. Sin regla de movimiento diagonal definida (candidata: exigir ambas aristas ortogonales abiertas, evita cortar esquina de pilar).
  - Ítem aparte, NO bloqueado por ADR-026/TP_WATCH: aplicar el fix de `resolve_move_grid_gen`/`resolve_spawn_near` (bitmask en vez de celda cruda) es seguro y aislado (solo corrige colisión cosmética del robapieles). Candidato de bajo riesgo/bajo esfuerzo para cualquier sesión futura, sin dependencia técnica del diagnóstico de TPs — pendiente de decidir prioridad.
  - Hallazgo aparte (auditoría previa, re-confirmado): `handle_spawn_world_chest` (`game_loop.rs`) y `World::spawn_corpse` (`world/corpse.rs`) aceptan la posición que manda el cliente sin validar walkability contra nada — no es el desajuste 2.5m/5m, es ausencia total de gate. Pendiente de decisión, sin tocar.
- (OBSOLETO con edge-based) Celda Wall solitaria/diagonal no emite pared — ya no aplica: las paredes son flags de arista, no se derivan de celdas Wall.
- (OBSOLETO con edge-based) Tile Solid en borde de chunk no emite pared exterior — ya no hay tiles Solid; cada chunk emite sus aristas N/E.
- StructureValidator.cs comenta `ProceduralWorldGenerator.TryPlaceStructures` (método retirado en edge-based); el validador sigue funcionando (autónomo, MiniJson). Comentario stale a corregir.
- Archivo WallGreedyMesherTests.cs contiene GridTileClassificationTests (rename diferido)
- Tests Network/RemotePlayer fallan (preexistente, no relacionado con grid)
- Follow-up pendiente del ack: StpPickupGranted (reliable) retransmite sin parar pese a ACK_DEADLINE — el ACK no detiene la retransmisión como debería. A investigar en la reliability layer (network/peer.rs `process_ack`/`collect_retransmits` + network/mod.rs `process_retransmits`). No bloquea ADR-017.
- Comentarios de código (GridChunkBuilder/WallGreedyMesher/GridTestWorld) y memoria etiquetan el sistema de tiles como "ADR-001"; el ADR-001 real (DECISIONS.md) es Unity+URP. Mal-etiquetado a corregir.
- ADR-011: la implementación divergió del texto del ADR en dos puntos, pendiente de ENMIENDA (DECISIONS.md es append-only, no se edita el ADR; se añade enmienda como en ADR-009): (a) el timestamp se selló en `NetworkManager.last_pickup_at`, NO en `Player` (motivo: NetworkManager está threadeado en los 3 puntos de sello/lectura; Player no llega sin cambiar firmas); (b) se añadió un sello ADICIONAL en la rama del joiner (`NetworkEvent::StpPickupGranted`) además del de `process_stp_pickup`, necesario porque en P2P multi-backend el joiner confirma su propio pickup ahí (sin él, un joiner que recoge no animaría en pantallas ajenas).
- ProxyPickupHook.cs conserva un doc-comment stale que dice "upper-body Pickup" (ahora es full-body) — corregir.
- Robapieles (ADR-016 slice 2 + ADR-017) — colisión contra el mundo EQUIVOCADO (hallazgo runtime 2026-06-19): la colisión del fantasma consulta el mundo del BACKEND (`generate_chunk`/`Level0Collision`/`SimChunkCache`). Pero lo que se RENDERIZA y contra lo que colisionan los JUGADORES es el `ChunkStreamer` CLIENT-SIDE (Unity/C#), un generador DISTINTO; las dos geometrías no coinciden. → El robapieles respeta paredes del backend que no existen en lo renderizado y atraviesa paredes visibles que el backend desconoce. Los jugadores no lo sufren (su render y colisión son el mismo ChunkStreamer). La "cero divergencia" de ADR-017 era intra-backend (sim-only == `generate_chunk`), no contempla backend↔ChunkStreamer. SE RESUELVE con la migración del mundo a backend-authoritative (el ChunkStreamer renderiza el mundo que genera el backend → render y colisión vuelven a coincidir, ahora server-authoritative); ver ▸ Riesgos abiertos. Hasta entonces el robapieles es PROTOTIPO con colisión imperfecta; los slices restantes (pickup imitado, slice 4) NO dependen de la colisión y se completan igual. Nota datada anexada a ADR-016 en DECISIONS.md.

## ADRs pendientes (numeración alineada con DECISIONS.md)
- **PENDIENTE (no bloqueante, 2026-07-04): esta lista está desactualizada desde ADR-021 — faltan ADR-022 a ADR-028 (todos VALIDADOS en DECISIONS.md). Rellenar en una sesión dedicada.**
- ADR-003: Topología de red (propuesta, ya en DECISIONS.md) — bloquea persistencia/regiones
- ADR-004: Formato de chunk y seams procedurales (pendiente, ya en DECISIONS.md)
- ADR-005: IPC cliente↔servidor (protocolo, tick rate, autoridad) — bloquea conectar load_profiles end-to-end
- ADR-006: Colisión Rust de celdas Wall (slab fino 0.2 m centrado)
- ADR-009: Protocolo player/stat/world + predicción cliente (migración STP) — VALIDADA (2026-06-13). Comparte transporte con ADR-005. Slice 3.1 (plumbing) implementada y verde; slice 3.2 (predicción L2) pendiente.
- ADR-012: AnimatorController custom de proxies (reemplaza el override vendor) — VALIDADA (2026-06-18), ya en DECISIONS.md.
- ADR-013: animación de proxies velocity-derived (locomoción + jump client-side, cero cambios de red) — VALIDADA (2026-06-18), ya en DECISIONS.md.
- ADR-011: ENMIENDA (2026-06-18) anexada reconciliando registro↔implementación (sello en NetworkManager + rama joiner).
- ADR-017: VALIDADA (2026-06-19) e IMPLEMENTADA — colisión sim-only on-demand (`SimChunkCache` + seam `LayoutSource` + `Level0Collision::resolve_move_simulated` en world/collision.rs). Separa el conjunto de colisión del de render: caché lateral host-only de layouts generados on-demand por el generador determinista, NUNCA insertados en world.chunks ni serializados a Unity. Path de colisión de jugadores/host INTACTO (wrappers pasan `LayoutSource::World`). Acotada por `SIM_CACHE_MAX_CHUNKS=64` + eviction farthest-from-query. API sin consumidor en producción hasta ADR-016 (ejercitada por tests). Prerrequisito de ADR-016.
- ADR-015: VALIDADA (2026-06-19) e IMPLEMENTADA (opción b) — propagación rot+anim de peers a clientes no-host vía reenvío host-only de `PlayerUpdate` con sender_id ajeno (`NetworkManager::send_unreliable_as` + `sync::broadcast_peer_poses`, cableado en game_loop a 10Hz dentro de `if net.is_host`). Sin schema, sin tocar PeerInfo/PeerList ni ipc/. CIERRE PENDIENTE del GATE DE TRÁFICO (play-test: medir datagramas/s del host a 10-15 peers; si P×J escala mal, acotar por proximidad ANTES de cerrar). Instrumentación incluida (traza MPTRACE `step=R15` ~1/s con peer_count + datagramas de relay/llamada). Prerrequisito de ADR-016.
- ADR-016: VALIDADA (2026-06-19) y ANEXADA a DECISIONS.md (+ notas datadas: limitación de colisión; identidad/tell/limpieza). ROBAPIELES BASE + IDENTIDAD/TELL COMPLETOS (prototipo). IMPLEMENTADO en backend (build+tests verdes): slice 1 (aparición), 2 (movimiento + colisión sim-only ADR-017, 1er uso runtime), 4 (pickup imitado anim-only — invariante verificada: jamás process_stp_pickup/pending_pickups/stp_items/grants), identidad de víctima (clona NOMBRE, ID propio 0xF000), tell #2 stare. Slice 3 (fidelidad en joiners) cabalga sobre ADR-015 → verificación de play-test. DEBUG_SPAWN_PHANTOM ahora env flag (OFF por defecto). PENDIENTE (fases futuras): regla de EXPOSICIÓN (no dos copias del mismo jugador), clonado de APARIENCIA/skin, re-selección dinámica de víctima, detección/persecución/IA con propósito, máquina de estados. DEUDA registrada: colisión contra mundo backend ≠ ChunkStreamer (no afecta identidad/tell; se resuelve con la migración del mundo a backend-authoritative). Depende de ADR-015 y ADR-017.

- ADR-020: Crouch sincronizado vía campo tipado `crouch:bool` en la pose (cosmético/host-relay; schema v2→v3) — VALIDADA (2026-06-29) e IMPLEMENTADA (backend+cliente, commits dcf3f1b/9037a99). Bakeado + play-tested (el proxy se agacha). Extiende el camino de pose de ADR-011 y el relay de ADR-015.

- ADR-021: Camera Pitch Sync vía campo tipado `pitch:i8` cuantizado a 1° en la pose relay (cosmético/host-relay; schema v3→v4) — VALIDADA (2026-06-30) e IMPLEMENTADA + bakeada + play-tested (el proxy inclina cabeza/cuello/spine). Reusa `PlayerInput.look[0]` para el input (sin cambio de entrada). Extiende ADR-011/020 y el relay de ADR-015.

## Decisiones recientes
- Ver docs/DECISIONS.md (ADR-001..007). ADR-007 aprobada (implementada parcial); ADR-005/006 propuestas.

## Riesgos abiertos
- ADR-003 (topología de red) sin validar: bloquea diseño de persistencia y regiones.
- ADR-007: params nuevos sin cablear al algoritmo y load_profiles sin conectar end-to-end (espera ADR-005 IPC).
- Fase 2 implementada (ADR-014 VALIDADA 2026-06-19): borrado diferido ~0.20s + reserva host-only anti-duplicado. Pendiente play-test. (El dedup de StpPickupGranted retransmitido — ADR-011 follow-up — también ya implementado.)
- Fase 3 (aim/look sync — rotación de cámara remota → orientación del proxy; requiere campo de rotación nuevo en el paquete de pose → cambio de schema → ADR) — bloqueada por cambio de protocolo.
- PROYECTO PENDIENTE GRANDE — Migración del mundo a backend-authoritative (identificado 2026-06-19, sin diseñar aún): hoy hay DOS mundos que no coinciden — el del backend (`generate_chunk`/`Level0Collision`) y el `ChunkStreamer` client-side (Unity, lo que se renderiza y contra lo que colisionan los jugadores). El objetivo: el backend pasa a ser la fuente de verdad del mundo y el `ChunkStreamer` renderiza ESE mundo (vía IPC/streaming de chunks, cf. ADR-004/005). Beneficios: (a) resuelve la colisión del robapieles (ver ▸ Deuda conocida) porque render y colisión vuelven a ser el mismo mundo; (b) hace el mundo server-authoritative (coherente con ADR-009 para stats/movimiento). Proyecto grande, requerirá su(s) propio(s) ADR (toca worldgen + IPC + render). Solo REGISTRADO como pendiente; no diseñado.

## NO tocar
- Modelo de datos Rust (celdas 2.5 m): la conversión celda→tile vive SOLO en Unity (tileX = cellX / 2).
- **Clases nativas de STP/PolymindGames — nunca editarlas directamente** (hallazgo 2026-07-01, durante ADR-025 Slice A). Restricción dura de Unity: `PolymindGames.asmdef` NO puede referenciar `Assembly-CSharp` (donde vive `_Migration/STPIntegration/`, sin asmdef propio) — un asmdef explícito no puede depender del assembly implícito por defecto. Esto significa que si una clase STP necesitara consumir una señal externa, la clase-puente tendría que vivir físicamente DENTRO del árbol de `Assets/PolymindGames/` para ser visible — el mecanismo tentador siempre va a "vivir dentro de PolymindGames" aunque sea código nuevo nuestro. **La regla no es "evitar esa carpeta", es evitar la necesidad**: cualquier interacción con STP debe ser un interceptor/wrapper EXTERNO que actúa DESPUÉS de que STP hace lo suyo (ej. `AuthoritativePoseApplier` deja que `SoloSurvivalGameMode.SetPlayerPosition` haga su teletransporte local nativo, y lo corrige un instante después vía la pose autoritativa) — nunca un guard/edit dentro del método STP, y nunca una clase-señal nueva colocada dentro de `Assets/PolymindGames/` para que STP la lea. Caso de estudio revertido: un guard `PlayerRespawnGate.RepositionLocally()` se añadió dentro de `SoloSurvivalGameMode.OnPlayerRespawn` + una clase gate nueva en `Assets/PolymindGames/FPSCore/Code/Runtime/Core/` — ambos revertidos; `AuthoritativePoseApplier` solo (sin tocar STP) cubre el mismo caso.
